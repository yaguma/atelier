/**
 * Card.ts - カードエンティティ（後方互換性用再エクスポート）
 *
 * @description
 * 実体は features/deck/types/card.ts に移動済み。
 * 後方互換性のため再エクスポートを提供する。
 *
 * 新規コードでは @features/deck/types を使用すること。
 */
export { Card } from '@features/deck/types/card';

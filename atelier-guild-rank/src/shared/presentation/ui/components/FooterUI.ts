/**
 * フッターUIコンポーネント（後方互換性用再エクスポート）
 *
 * @description
 * 実体は shared/components/FooterUI.ts に移動済み。
 * 後方互換性のため再エクスポートを提供する。
 *
 * 新規コードでは @shared/components を使用すること。
 */
export { FooterUI, type PhaseIndicatorState } from '@shared/components/FooterUI';

/**
 * deck-service.interface.ts - DeckServiceインターフェース（後方互換性用再エクスポート）
 *
 * @description
 * 実体は features/deck/types/deck-service.ts に移動済み。
 * 後方互換性のため再エクスポートを提供する。
 *
 * 新規コードでは @features/deck/types を使用すること。
 */
export type { IDeckService } from '@features/deck/types/deck-service';

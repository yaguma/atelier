/**
 * TitleScene.ts - タイトルシーン
 * TASK-0008: Phaser基本設定とBootScene
 *
 * 【機能概要】: ゲームのタイトル画面を表示する仮実装シーン 🔵
 * 【実装方針】: 最小限の実装でテストを通す（詳細実装は後続タスクで行う） 🔵
 * 【テスト対応】: T-0008-02（BootSceneからの遷移）のテストを通す 🔵
 */

import Phaser from 'phaser';

/**
 * TitleScene - タイトル画面シーン
 *
 * 【責務】:
 * - タイトルロゴの表示
 * - ゲーム開始ボタンの表示（仮実装）
 * - MainSceneへの遷移
 *
 * 🔵 信頼性レベル: note.mdのTitleScene仮実装例に記載
 */
export class TitleScene extends Phaser.Scene {
  constructor() {
    super({ key: 'TitleScene' });
  }

  /**
   * create() - タイトル画面の生成
   *
   * 【機能概要】: タイトルロゴとスタートボタンを表示する
   * 【実装方針】: 最小限のテキスト表示のみ（UI構築は後続タスクで実装）
   * 【テスト対応】: T-0008-02-E2E（E2Eテストでタイトル表示確認）のテストを通す
   * 🔵 信頼性レベル: note.mdのTitleScene仮実装例に記載
   */
  create(): void {
    // 【画面中央座標取得】: テキストを中央に配置するための座標を取得 🔵
    const centerX = this.cameras.main.centerX;
    const centerY = this.cameras.main.centerY;

    // 【タイトルロゴ表示】: "Atelier Guild Rank"テキストを表示 🔵
    // 【実装内容】: ゲームタイトルを大きく表示
    this.add
      .text(centerX, centerY - 50, 'Atelier Guild Rank', {
        fontSize: '48px',
        color: '#8B4513', // ブラウン色（錬金術らしい色）
      })
      .setOrigin(0.5);

    // 【スタート案内表示】: "Press SPACE to start"テキストを表示 🔵
    // 【実装内容】: スペースキーでゲーム開始を案内
    this.add
      .text(centerX, centerY + 50, 'Press SPACE to start', {
        fontSize: '24px',
        color: '#666666',
      })
      .setOrigin(0.5);

    // 【キーボード入力監視】: スペースキーでMainSceneへ遷移 🔵
    // 【実装内容】: 仮実装のため、スペースキーで直接MainSceneへ遷移
    this.input.keyboard?.once('keydown-SPACE', () => {
      // 【シーン遷移】: MainSceneへ遷移 🔵
      this.scene.start('MainScene');
    });
  }
}

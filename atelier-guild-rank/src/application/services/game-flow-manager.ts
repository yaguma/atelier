/**
 * game-flow-manager.ts（後方互換性用再エクスポート）
 *
 * @description
 * 実体は shared/services/game-flow/game-flow-manager.ts に移動済み。
 * 後方互換性のため再エクスポートを提供する。
 *
 * 新規コードでは @shared/services を使用すること。
 */
export { GameFlowManager } from '@shared/services/game-flow/game-flow-manager';

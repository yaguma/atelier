# TASK-0039: ボタンホバーエフェクト強化

**タスクID**: TASK-0039
**タスクタイプ**: DIRECT
**推定工数**: 4時間
**フェーズ**: Phase 5 - UI強化・ポリッシュ
**信頼性レベル**: 🟡 *TASK-0019の推奨条件より*

## 関連文書

- **概要**: [📋 overview.md](../overview.md)
- **元タスク**: [📋 TASK-0019.md](../phase-3/TASK-0019.md) - TitleScene推奨条件
- **UI設計**: [📐 ui-design/overview.md](../../design/atelier-guild-rank/ui-design/overview.md)

## タスク概要

ボタンコンポーネントのホバーエフェクトを強化する。マウスオーバー時の視覚フィードバック（拡大、色変更、光彩効果など）を実装し、UIのインタラクティブ性を向上させる。

## 依存タスク

- **前提タスク**: [TASK-0018: 共通UIコンポーネント基盤](../phase-3/TASK-0018.md) ✅完了
- **後続タスク**: なし（独立タスク）

## 完了条件

- [x] ホバー時の拡大アニメーション実装
- [x] ホバー時の色変更実装
- [x] 押下時のフィードバック実装
- [x] 無効状態でのホバー無効化
- [x] 動作確認完了

---

## 実装詳細

### 1. ホバー拡大アニメーション 🟡

**信頼性**: 🟡 *既存CardUI実装から推測*

**実装ファイル**: `src/presentation/ui/components/Button.ts`

```typescript
// ホバー時の拡大
private onPointerOver(): void {
  if (this.isDisabled) return;

  this.scene.tweens.add({
    targets: this.container,
    scaleX: 1.05,
    scaleY: 1.05,
    duration: 100,
    ease: 'Power2',
  });

  this.setHighlight(true);
}

private onPointerOut(): void {
  this.scene.tweens.add({
    targets: this.container,
    scaleX: 1,
    scaleY: 1,
    duration: 100,
    ease: 'Power2',
  });

  this.setHighlight(false);
}
```

### 2. 色変更エフェクト 🟡

**信頼性**: 🟡 *既存テーマ定義から推測*

**実装ファイル**: `src/presentation/ui/components/Button.ts`

```typescript
// ホバー時の色変更
private setHighlight(enabled: boolean): void {
  const color = enabled
    ? THEME.colors.primaryHover
    : THEME.colors.primary;
  this.background.setFillStyle(color);
}
```

### 3. 押下フィードバック 🟡

**信頼性**: 🟡 *標準的なUIパターンから推測*

```typescript
// 押下時のフィードバック
private onPointerDown(): void {
  if (this.isDisabled) return;

  this.scene.tweens.add({
    targets: this.container,
    scaleX: 0.95,
    scaleY: 0.95,
    duration: 50,
  });
}

private onPointerUp(): void {
  this.scene.tweens.add({
    targets: this.container,
    scaleX: 1.05,
    scaleY: 1.05,
    duration: 50,
  });
}
```

### 4. テーマ定義の拡張 🔵

**信頼性**: 🔵 *既存theme.ts構造を拡張*

**実装ファイル**: `src/presentation/ui/theme.ts`

```typescript
export const THEME = {
  colors: {
    // 既存定義...
    primaryHover: 0x9B5523,  // primary より明るい色
    secondaryHover: 0xE2792E,
  },
  // ...
};
```

---

## 実装手順

### DIRECTタスクの場合

1. `/tsumiki:direct-setup` - 直接実装・設定
2. `/tsumiki:direct-verify` - 動作確認・品質確認

---

## 注意事項

- 無効状態のボタンではホバーエフェクトを無効にする
- アニメーションは高速（100ms以下）で応答性を重視
- 既存のButtonコンポーネントとの互換性を維持

---

## 信頼性レベルサマリー

- **総項目数**: 4項目
- 🔵 **青信号**: 1項目 (25%)
- 🟡 **黄信号**: 3項目 (75%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質

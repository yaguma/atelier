# TASK-0032: ビルド・デプロイ設定

**バージョン**: 1.0.0
**作成日**: 2026-01-16
**フェーズ**: 4 - 統合・テスト
**見積時間**: 4時間（半日）
**依存タスク**: TASK-0030, TASK-0031

---

## 1. タスク概要

| 項目 | 値 |
|------|-----|
| **タスクID** | TASK-0032 |
| **タスク名** | ビルド・デプロイ設定 |
| **カテゴリ** | インフラ |
| **優先度** | 中 |
| **担当レイヤー** | Infrastructure |

### 信頼性レベル

- 🔵 **青信号**: 設計文書に記載
- 🟡 **黄信号**: 設計文書から妥当な推測
- 🔴 **赤信号**: 設計文書にない推測

---

## 2. 実装内容

### 2.1 本番ビルド設定 🟡

`vite.config.ts`:

```typescript
export default defineConfig({
  build: {
    outDir: 'dist',
    sourcemap: false,
    minify: 'terser',
    terserOptions: {
      compress: {
        drop_console: true,
        drop_debugger: true,
      },
    },
    rollupOptions: {
      output: {
        manualChunks: {
          phaser: ['phaser'],
          rexui: ['phaser3-rex-plugins'],
        },
      },
    },
  },
});
```

### 2.2 環境変数設定 🟡

`.env.production`:
```
VITE_APP_VERSION=1.0.0
VITE_APP_ENV=production
```

`.env.development`:
```
VITE_APP_VERSION=dev
VITE_APP_ENV=development
VITE_DEBUG_ENABLED=true
```

### 2.3 GitHub Actions CI/CD 🔴

`.github/workflows/deploy.yml`:

```yaml
name: Deploy

on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: npm ci
      - run: npm run lint
      - run: npm run test
      - run: npm run build
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
```

### 2.4 ビルド最適化 🟡

- 画像圧縮
- コード分割
- キャッシュ設定

---

## 3. 受け入れ基準

### 3.1 必須条件 🔵

- [x] 本番ビルドが成功する
- [ ] ビルド成果物が正常に動作する（GitHub Pagesへのデプロイ後に確認）
- [x] コンソールログが出力されない（terserOptions.compress.drop_console設定済み）

### 3.2 推奨条件 🟡

- [x] CI/CDパイプライン動作（.github/workflows/deploy.yml作成済み）
- [x] ビルドサイズが10MB以下（2.4MB）

---

## 4. テストケース

### 4.1 ビルドテスト

| テストID | テスト内容 | 期待結果 |
|---------|----------|----------|
| T-0032-01 | 本番ビルド | エラーなし |
| T-0032-02 | ビルド成果物起動 | 正常動作 |
| T-0032-03 | コンソール出力 | デバッグログなし |
| T-0032-04 | ビルドサイズ | 10MB以下 |

---

## 5. 成果物

| 成果物 | パス |
|--------|------|
| Vite設定 | `/vite.config.ts` |
| 環境変数 | `/.env.production`, `/.env.development` |
| CI/CD設定 | `/.github/workflows/deploy.yml` |

---

## 関連文書

- **アーキテクチャ設計書**: [../../design/atelier-guild-rank/architecture-overview.md](../../design/atelier-guild-rank/architecture-overview.md)

---

## 変更履歴

| 日付 | バージョン | 変更内容 |
|------|----------|---------|
| 2026-01-16 | 1.0.0 | 初版作成 |
| 2026-01-19 | 1.1.0 | DIRECT実装完了（Vite設定、環境変数、GitHub Actions設定） |

# TASK-0003: 共通型定義

**バージョン**: 1.0.0
**作成日**: 2026-01-16
**フェーズ**: 1 - 基盤構築
**見積時間**: 4時間（半日）
**依存タスク**: TASK-0002

---

## 1. タスク概要

| 項目 | 値 |
|------|-----|
| **タスクID** | TASK-0003 |
| **タスク名** | 共通型定義 |
| **カテゴリ** | 基盤構築 |
| **優先度** | 最高 |
| **担当レイヤー** | shared/types |

### 信頼性レベル

- 🔵 **青信号**: 設計文書に記載
- 🟡 **黄信号**: 設計文書から妥当な推測
- 🔴 **赤信号**: 設計文書にない推測

---

## 2. 実装内容

### 2.1 基本型定義 🔵

`src/shared/types/common.ts`:

```typescript
// ランク型
export type GuildRank = 'G' | 'F' | 'E' | 'D' | 'C' | 'B' | 'A' | 'S';

// 品質型
export type Quality = 'D' | 'C' | 'B' | 'A' | 'S';

// フェーズ型
export type GamePhase = 'quest_accept' | 'gathering' | 'alchemy' | 'delivery';

// カード種別
export type CardType = 'gathering' | 'alchemy' | 'utility';

// 素材カテゴリ
export type MaterialCategory = 'plant' | 'mineral' | 'animal' | 'magic';

// 依頼者種別
export type ClientType = 'villager' | 'adventurer' | 'merchant' | 'noble' | 'guild';
```

### 2.2 エンティティID型 🔵

`src/shared/types/ids.ts`:

```typescript
// ブランド型によるID定義
export type CardId = string & { readonly brand: unique symbol };
export type MaterialId = string & { readonly brand: unique symbol };
export type ItemId = string & { readonly brand: unique symbol };
export type QuestId = string & { readonly brand: unique symbol };
export type ArtifactId = string & { readonly brand: unique symbol };
export type ClientId = string & { readonly brand: unique symbol };
```

### 2.3 ゲーム状態型 🔵

`src/shared/types/game-state.ts`:

```typescript
export interface GameState {
  currentRank: GuildRank;
  promotionGauge: number;
  remainingDays: number;
  gold: number;
  actionPoints: number;
  maxActionPoints: number;
  currentPhase: GamePhase;
  currentDay: number;
}
```

### 2.4 マスターデータ型 🔵

`src/shared/types/master-data.ts`:

```typescript
export interface CardMaster {
  id: CardId;
  name: string;
  type: CardType;
  cost: number;
  effect: CardEffect;
  description: string;
}

export interface MaterialMaster {
  id: MaterialId;
  name: string;
  category: MaterialCategory;
  baseQuality: Quality;
  rarity: number;
}

export interface ItemMaster {
  id: ItemId;
  name: string;
  category: string;
  recipe: RecipeRequirement[];
  basePrice: number;
}
```

---

## 3. 受け入れ基準

### 3.1 必須条件 🔵

- [ ] interfaces.tsに定義された全型が実装されている
- [ ] 型エラーなくコンパイルが通る
- [ ] ブランド型でIDの誤用を防止している

### 3.2 推奨条件 🟡

- [ ] JSDocコメントが付与されている
- [ ] index.tsで再エクスポートされている

---

## 4. テストケース

### 4.1 型テスト

| テストID | テスト内容 | 期待結果 |
|---------|----------|----------|
| T-0003-01 | GuildRank型の有効値 | 'G'〜'S'のみ受け付け |
| T-0003-02 | Quality型の有効値 | 'D'〜'S'のみ受け付け |
| T-0003-03 | CardId型の型安全性 | 異なるID型の混同を検出 |

---

## 5. 成果物

| 成果物 | パス |
|--------|------|
| 共通型定義 | `/src/shared/types/common.ts` |
| ID型定義 | `/src/shared/types/ids.ts` |
| ゲーム状態型 | `/src/shared/types/game-state.ts` |
| マスターデータ型 | `/src/shared/types/master-data.ts` |
| index.ts | `/src/shared/types/index.ts` |

---

## 関連文書

- **インターフェース定義**: [../../design/atelier-guild-rank/interfaces.ts](../../design/atelier-guild-rank/interfaces.ts)
- **データスキーマ**: [../../design/atelier-guild-rank/data-schema-master-game.md](../../design/atelier-guild-rank/data-schema-master-game.md)

---

## 変更履歴

| 日付 | バージョン | 変更内容 |
|------|----------|---------|
| 2026-01-16 | 1.0.0 | 初版作成 |

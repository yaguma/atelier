# TASK-0008: Phaser基本設定とBootScene

**バージョン**: 1.0.0
**作成日**: 2026-01-16
**フェーズ**: 1 - 基盤構築
**見積時間**: 4時間（半日）
**依存タスク**: TASK-0002, TASK-0006

---

## 1. タスク概要

| 項目 | 値 |
|------|-----|
| **タスクID** | TASK-0008 |
| **タスク名** | Phaser基本設定とBootScene |
| **カテゴリ** | 基盤構築 |
| **優先度** | 高 |
| **担当レイヤー** | Presentation |

### 信頼性レベル

- 🔵 **青信号**: 設計文書に記載
- 🟡 **黄信号**: 設計文書から妥当な推測
- 🔴 **赤信号**: 設計文書にない推測

---

## 2. 実装内容

### 2.1 Phaser設定 🔵

`src/main.ts`:

```typescript
import Phaser from 'phaser';
import RexUIPlugin from 'phaser3-rex-plugins/templates/ui/ui-plugin';
import { BootScene } from '@presentation/scenes/BootScene';
import { TitleScene } from '@presentation/scenes/TitleScene';
import { MainScene } from '@presentation/scenes/MainScene';

const config: Phaser.Types.Core.GameConfig = {
  type: Phaser.AUTO,
  width: 1280,
  height: 720,
  parent: 'game-container',
  backgroundColor: '#F5F5DC', // Beige
  scene: [BootScene, TitleScene, MainScene],
  plugins: {
    scene: [
      {
        key: 'rexUI',
        plugin: RexUIPlugin,
        mapping: 'rexUI',
      },
    ],
  },
  scale: {
    mode: Phaser.Scale.FIT,
    autoCenter: Phaser.Scale.CENTER_BOTH,
  },
};

new Phaser.Game(config);
```

### 2.2 BootScene実装 🔵

`src/presentation/scenes/BootScene.ts`:

```typescript
export class BootScene extends Phaser.Scene {
  constructor() {
    super({ key: 'BootScene' });
  }

  preload(): void {
    // マスターデータ読み込み
    // プログレスバー表示
  }

  create(): void {
    // サービス初期化
    // TitleSceneへ遷移
    this.scene.start('TitleScene');
  }
}
```

### 2.3 シーン基底クラス 🟡

`src/presentation/scenes/BaseScene.ts`:

```typescript
export abstract class BaseScene extends Phaser.Scene {
  protected rexUI!: RexUIPlugin;
  protected eventBus!: IEventBus;

  init(): void {
    // 共通初期化
  }

  abstract create(): void;
}
```

### 2.4 DIコンテナ設定 🟡

`src/infrastructure/di/container.ts`:

- サービスの登録
- シングルトン管理

---

## 3. 受け入れ基準

### 3.1 必須条件 🔵

- [ ] Phaserが起動する
- [ ] rexUIプラグインが利用可能
- [ ] BootSceneからTitleSceneへ遷移する

### 3.2 推奨条件 🟡

- [ ] ローディングプログレスバー表示
- [ ] マスターデータ読み込み完了後に遷移

---

## 4. テストケース

### 4.1 統合テスト

| テストID | テスト内容 | 期待結果 |
|---------|----------|----------|
| T-0008-01 | ゲーム起動 | エラーなく起動 |
| T-0008-02 | シーン遷移 | TitleScene表示 |
| T-0008-03 | rexUIプラグイン | 利用可能 |

---

## 5. 成果物

| 成果物 | パス |
|--------|------|
| エントリーポイント | `/src/main.ts` |
| BootScene | `/src/presentation/scenes/BootScene.ts` |
| BaseScene | `/src/presentation/scenes/BaseScene.ts` |
| DIコンテナ | `/src/infrastructure/di/container.ts` |
| HTML | `/index.html` |

---

## 関連文書

- **アーキテクチャ設計書**: [../../design/atelier-guild-rank/architecture-overview.md](../../design/atelier-guild-rank/architecture-overview.md)
- **UI設計概要**: [../../design/atelier-guild-rank/ui-design/overview.md](../../design/atelier-guild-rank/ui-design/overview.md)

---

## 変更履歴

| 日付 | バージョン | 変更内容 |
|------|----------|---------|
| 2026-01-16 | 1.0.0 | 初版作成 |

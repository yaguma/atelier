# TASK-0035: Tailwind CSS設定

**タスクID**: TASK-0035
**タスクタイプ**: DIRECT
**推定工数**: 4時間
**フェーズ**: Phase 1 - 基盤構築
**信頼性レベル**: 🔵 *architecture-overview.md v2.2.0より*

## 関連文書

- **概要**: [📋 overview.md](../overview.md)
- **設計文書**: [📐 architecture-overview.md](../../../design/atelier-guild-rank/architecture-overview.md)

## タスク概要

Tailwind CSS 4.xを設定し、オーバーレイUI（メニュー、ダイアログ、設定画面等）用のスタイリング基盤を構築する。ゲーム本体のCanvas UIはPhaserが担当し、HTML要素によるオーバーレイUIはTailwind CSSでスタイリングする。

## 依存タスク

- **前提タスク**: [TASK-0002: ディレクトリ構造作成](TASK-0002.md) ✅
- **後続タスク**: Phase 3のUI実装タスク（TASK-0026等でオーバーレイUIを使用する場合）

## 完了条件

- [ ] Tailwind CSS 4.xがインストールされている
- [ ] tailwind.config.jsが設定済み
- [ ] postcss.config.jsが設定済み
- [ ] メインCSSファイルにTailwindディレクティブが設定済み
- [ ] Viteビルドでtailwind CSSが正常に処理される
- [ ] ゲーム用カラーパレットが定義されている

---

## 実装詳細

### 1. Tailwind CSSインストール 🔵

**信頼性**: 🔵 *architecture-overview.md セクション1.1, 1.5より*

```bash
pnpm add -D tailwindcss @tailwindcss/vite postcss autoprefixer
```

### 2. tailwind.config.js作成 🔵

**信頼性**: 🔵 *architecture-overview.md セクション1.5より*

```javascript
// tailwind.config.js
/** @type {import('tailwindcss').Config} */
export default {
  content: [
    './index.html',
    './src/**/*.{ts,tsx,html}',
  ],
  theme: {
    extend: {
      colors: {
        // ゲームのカラーパレット
        'atelier': {
          'primary': '#4a5568',
          'secondary': '#718096',
          'accent': '#ed8936',
          'gold': '#d69e2e',
          'rank-g': '#a0aec0',
          'rank-f': '#68d391',
          'rank-e': '#48bb78',
          'rank-d': '#38a169',
          'rank-c': '#3182ce',
          'rank-b': '#805ad5',
          'rank-a': '#d53f8c',
          'rank-s': '#ecc94b',
        },
        'game': {
          'bg': '#1a202c',
          'panel': '#2d3748',
          'border': '#4a5568',
          'text': '#e2e8f0',
          'text-muted': '#a0aec0',
        }
      },
      fontFamily: {
        'game': ['"M PLUS Rounded 1c"', 'sans-serif'],
      },
      boxShadow: {
        'game': '0 4px 6px -1px rgba(0, 0, 0, 0.3), 0 2px 4px -1px rgba(0, 0, 0, 0.2)',
        'game-lg': '0 10px 15px -3px rgba(0, 0, 0, 0.3), 0 4px 6px -2px rgba(0, 0, 0, 0.2)',
      }
    },
  },
  plugins: [],
}
```

**実装ファイル**: `tailwind.config.js`

### 3. postcss.config.js作成 🔵

**信頼性**: 🔵 *標準的なPostCSS設定*

```javascript
// postcss.config.js
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
```

**実装ファイル**: `postcss.config.js`

### 4. Vite設定更新 🔵

**信頼性**: 🔵 *Vite + Tailwind統合*

```typescript
// vite.config.ts
import { defineConfig } from 'vite';
import tailwindcss from '@tailwindcss/vite';

export default defineConfig({
  plugins: [
    tailwindcss(),
  ],
  // ...既存設定
});
```

**実装ファイル**: `vite.config.ts`

### 5. メインCSSファイル作成 🔵

**信頼性**: 🔵 *architecture-overview.md セクション1.5より*

```css
/* src/styles/main.css */
@tailwind base;
@tailwind components;
@tailwind utilities;

/* ゲーム固有のベーススタイル */
@layer base {
  body {
    @apply bg-game-bg text-game-text overflow-hidden;
    font-family: 'M PLUS Rounded 1c', sans-serif;
  }

  /* Phaserキャンバスを全画面表示 */
  #game-container {
    @apply w-full h-screen;
  }
}

/* オーバーレイUI用コンポーネント */
@layer components {
  .overlay-panel {
    @apply absolute bg-game-panel/90 rounded-lg p-4 shadow-game backdrop-blur-sm border border-game-border;
  }

  .game-button {
    @apply px-4 py-2 bg-atelier-accent hover:bg-orange-500
           text-white font-game rounded transition-colors
           shadow-game active:shadow-none;
  }

  .game-button-secondary {
    @apply px-4 py-2 bg-game-panel hover:bg-game-border
           text-game-text font-game rounded transition-colors
           border border-game-border;
  }

  .dialog-overlay {
    @apply fixed inset-0 bg-black/50 flex items-center justify-center z-50;
  }

  .dialog-content {
    @apply bg-game-panel rounded-lg p-6 shadow-game-lg border border-game-border
           max-w-md w-full mx-4;
  }
}
```

**実装ファイル**: `src/styles/main.css`

### 6. index.htmlにCSS読み込み追加 🔵

**信頼性**: 🔵 *標準的な設定*

```html
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>アトリエ - ギルドランク制デッキ構築RPG</title>
  <link rel="stylesheet" href="/src/styles/main.css">
</head>
<body>
  <div id="game-container"></div>
  <script type="module" src="/src/main.ts"></script>
</body>
</html>
```

**実装ファイル**: `index.html`

---

## テスト要件

### 動作確認テスト 🔵

**信頼性**: 🔵 *標準的な動作確認*

1. **ビルド確認**
   - `pnpm dev` でTailwindが正常に処理される
   - `pnpm build` でCSSが正しくバンドルされる

2. **スタイル適用確認**
   - `.overlay-panel` クラスがスタイル適用される
   - `.game-button` クラスがスタイル適用される
   - カスタムカラー（atelier-accent等）が使用可能

3. **Phaserとの共存確認**
   - Phaserキャンバスが全画面表示される
   - オーバーレイ要素がキャンバス上に表示可能

---

## 実装手順

### DIRECTタスク

1. `/tsumiki:direct-setup` - Tailwind CSS設定
2. `/tsumiki:direct-verify` - 動作確認

---

## 注意事項

- Phaserのキャンバス内UIはrexUIを使用し、Tailwindは使用しない
- オーバーレイUIは設定画面、ポーズメニュー、確認ダイアログ等に使用
- Google Fonts（M PLUS Rounded 1c）の読み込みは別途必要

---

## 信頼性レベルサマリー

### 項目別信頼性

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 6 | 0 | 0 | 6 |
| テスト要件 | 1 | 0 | 0 | 1 |

### 全体評価

- **総項目数**: 7項目
- 🔵 **青信号**: 7項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質

# TASK-0054: テーマ定数統一（カラー・アニメーション）

**タスクID**: TASK-0054
**タスクタイプ**: DIRECT
**推定工数**: 4時間
**フェーズ**: Phase 7 - Presentation層リファクタリング
**信頼性レベル**: 🟡 *コードベース調査結果より*

## 関連文書

- **概要**: [📋 overview.md](../overview.md)
- **設計文書**: [📐 architecture-overview.md](../../design/atelier-guild-rank/architecture-overview.md)

## タスク概要

各UIコンポーネントで個別に定義されているカラー定数とアニメーション定数を一元化する。テーマファイル（theme.ts）の拡張と、AnimationPresetsの作成を行う。

## 依存タスク

- **前提タスク**: [TASK-0053: 共通UIユーティリティ基盤作成](TASK-0053.md)
- **後続タスク**: [TASK-0055](TASK-0055.md), [TASK-0056](TASK-0056.md), [TASK-0057](TASK-0057.md), [TASK-0058](TASK-0058.md)

## 完了条件

- [x] theme.ts にカラーパレットが統一定義されている
- [x] AnimationPresets.ts が作成されている
- [x] 既存コンポーネントが新しい定数を参照している（最低3箇所）
- [x] 既存テストが全て通過する

---

## 実装詳細

### 1. カラーパレット統一 🟡

**信頼性**: 🟡 *コードベース調査から妥当な推測*

既存のtheme.tsを拡張し、分散しているカラー定義を統合する。

**実装ファイル**: `src/presentation/theme.ts`

```typescript
export const Colors = {
  // 背景色
  background: {
    primary: 0x2a2a3d,
    secondary: 0x1a1a2e,
    overlay: 0x000000,
    card: 0x3a3a4d,
  },

  // ボーダー色
  border: {
    primary: 0x4a4a5d,
    secondary: 0x5a5a6d,
    highlight: 0x6a6a7d,
    gold: 0xffd700,
  },

  // テキスト色
  text: {
    primary: 0xffffff,
    secondary: 0xcccccc,
    muted: 0x888888,
    accent: 0xffd700,
    error: 0xff4444,
    success: 0x44ff44,
  },

  // 品質色（アイテム・素材）
  quality: {
    common: 0xcccccc,
    uncommon: 0x44ff44,
    rare: 0x4444ff,
    epic: 0xaa44ff,
    legendary: 0xffaa00,
  },

  // UI要素色
  ui: {
    button: {
      normal: 0x4a4a5d,
      hover: 0x5a5a6d,
      active: 0x6a6a7d,
      disabled: 0x2a2a3d,
    },
    progress: {
      background: 0x2a2a3d,
      fill: 0x4a9eff,
      warning: 0xffaa00,
      danger: 0xff4444,
    },
  },
} as const;

export type ColorKey = keyof typeof Colors;
```

### 2. アニメーションプリセット作成 🟡

**信頼性**: 🟡 *コードベース調査から妥当な推測*

各コンポーネントで重複しているアニメーション設定を統一する。

**実装ファイル**: `src/presentation/ui/utils/AnimationPresets.ts`

```typescript
export const AnimationPresets = {
  // フェード
  fade: {
    in: { alpha: { from: 0, to: 1 }, duration: 300, ease: 'Power2' },
    out: { alpha: { from: 1, to: 0 }, duration: 300, ease: 'Power2' },
    quick: { duration: 150 },
    slow: { duration: 500 },
  },

  // スケール
  scale: {
    pop: { scale: { from: 0.8, to: 1 }, duration: 200, ease: 'Back.easeOut' },
    hover: { scale: 1.05, duration: 150, ease: 'Power2' },
    press: { scale: 0.95, duration: 100, ease: 'Power2' },
    bounce: { scale: { from: 0.5, to: 1 }, duration: 400, ease: 'Bounce.easeOut' },
  },

  // 移動
  slide: {
    left: { x: '-=100', duration: 300, ease: 'Power2' },
    right: { x: '+=100', duration: 300, ease: 'Power2' },
    up: { y: '-=100', duration: 300, ease: 'Power2' },
    down: { y: '+=100', duration: 300, ease: 'Power2' },
  },

  // カード
  card: {
    draw: { y: '-=50', alpha: { from: 0, to: 1 }, duration: 300, ease: 'Power2' },
    discard: { y: '+=50', alpha: { from: 1, to: 0 }, duration: 200, ease: 'Power2' },
    flip: { scaleX: { from: 1, to: 0, yoyo: true }, duration: 300 },
    select: { scale: 1.1, duration: 150, ease: 'Back.easeOut' },
  },

  // ボタン
  button: {
    hover: { scale: 1.05, duration: 100, ease: 'Power2' },
    click: { scale: 0.95, duration: 50, ease: 'Power2' },
    release: { scale: 1, duration: 100, ease: 'Power2' },
  },

  // タイミング定数
  timing: {
    instant: 0,
    fast: 100,
    normal: 200,
    slow: 400,
    verySlow: 800,
  },
} as const;

export type AnimationPresetKey = keyof typeof AnimationPresets;
```

### 3. 既存コンポーネントの移行（サンプル） 🟡

**信頼性**: 🟡 *リファクタリング作業*

最低3箇所のコンポーネントで新しい定数を使用するように変更する。

**対象ファイル例**:
- `src/presentation/ui/components/CardUI.ts`
- `src/presentation/ui/components/QuestCardUI.ts`
- `src/presentation/ui/phases/QuestAcceptPhaseUI.ts`

```typescript
// Before
const CARD_BG_COLOR = 0x2a2a3d;
const HOVER_SCALE = 1.05;

// After
import { Colors } from '@presentation/theme';
import { AnimationPresets } from '@presentation/ui/utils/AnimationPresets';

const cardBgColor = Colors.background.card;
const hoverConfig = AnimationPresets.button.hover;
```

### 4. インデックスファイル更新 🔵

**信頼性**: 🔵 *標準的な実装パターン*

**実装ファイル**: `src/presentation/ui/utils/index.ts`

```typescript
export { AnimationPresets } from './AnimationPresets';
export type { AnimationPresetKey } from './AnimationPresets';
```

---

## 単体テスト要件

### テストケース1: Colors定義の検証 🔵

**信頼性**: 🔵 *型チェックで検証可能*

**Given**: Colorsオブジェクト
**When**: 各プロパティにアクセス
**Then**: 正しい数値が返される

### テストケース2: AnimationPresets定義の検証 🔵

**信頼性**: 🔵 *型チェックで検証可能*

**Given**: AnimationPresetsオブジェクト
**When**: 各プリセットにアクセス
**Then**: 正しいアニメーション設定が返される

---

## 実装手順

### DIRECTタスクの場合

1. `/tsumiki:direct-setup` - 直接実装・設定
2. `/tsumiki:direct-verify` - 動作確認・品質確認

---

## 注意事項

- 既存の動作を壊さないよう、段階的に移行する
- マジックナンバーを定数に置き換える際は、元の値を正確に保持する
- TypeScriptの型安全性を活用する（as const）

---

## 信頼性レベルサマリー

### 項目別信頼性

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 1 | 3 | 0 | 4 |
| 単体テスト | 2 | 0 | 0 | 2 |

### 全体評価

- **総項目数**: 6項目
- 🔵 **青信号**: 3項目 (50%)
- 🟡 **黄信号**: 3項目 (50%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質（標準的なリファクタリング作業）

# TASK-0086: features/inventory/components作成

## 基本情報

| 項目 | 値 |
|------|-----|
| **タスクID** | TASK-0086 |
| **タスク名** | features/inventory/components作成 |
| **見積時間** | 4時間 |
| **依存タスク** | TASK-0067, TASK-0085 |
| **開発タイプ** | TDD |
| **フェーズ** | Phase 7 - features/inventory機能 |
| **状態** | 🔲 未着手 |

---

## 概要

### 背景

インベントリ関連のUIコンポーネントを`src/features/inventory/components/`に集約する必要がある。Feature-Based Architectureにより、関連するUIコンポーネントを機能単位でまとめることで、保守性と再利用性が向上する。

### 目的

ItemInventoryUI、MaterialView等のインベントリ関連UIコンポーネントを移動・整理する。

### 現状

- UIコンポーネントがpresentation層に分散している可能性
- インベントリ固有のコンポーネントが他の機能と混在している可能性

---

## 要件

### 機能要件

| 要件ID | 要件 | 信頼性 |
|--------|------|--------|
| REQ-086-01 | ItemInventoryUIコンポーネントを移動する | 🔵 |
| REQ-086-02 | MaterialViewコンポーネントを移動する | 🔵 |
| REQ-086-03 | インベントリリスト表示コンポーネントを移動する | 🔵 |
| REQ-086-04 | 素材詳細表示コンポーネントを移動する | 🔵 |
| REQ-086-05 | コンポーネントのエクスポートを整理する | 🔵 |

### 非機能要件

| 要件ID | 要件 |
|--------|------|
| NFR-086-01 | 既存のUI動作が維持されること |
| NFR-086-02 | コンポーネントがfeatures/inventory/servicesを使用すること |
| NFR-086-03 | shared/ui-componentsの基盤コンポーネントを活用すること |

---

## テストケース

### コンポーネントテスト

```typescript
describe('features/inventory/components', () => {
  describe('ItemInventoryUI', () => {
    it('インベントリ一覧が正しく表示されること', () => {});
    it('アイテム選択時にコールバックが呼ばれること', () => {});
    it('空のインベントリ時に適切なメッセージが表示されること', () => {});
  });

  describe('MaterialView', () => {
    it('素材情報が正しく表示されること', () => {});
    it('素材の数量が正しく表示されること', () => {});
    it('レアリティに応じたスタイルが適用されること', () => {});
  });

  describe('コンポーネント統合', () => {
    it('servicesから取得したデータを正しく表示すること', () => {});
    it('フィルタリング機能が動作すること', () => {});
  });
});
```

---

## 実装計画

### ステップ1: 既存コンポーネント調査（1時間）

1. 既存のインベントリ関連UIコンポーネントを特定
2. 依存関係を調査
3. 移動対象のファイルリストを作成

### ステップ2: コンポーネント移動（2時間）

1. `src/features/inventory/components/`にファイルを作成
2. 既存コンポーネントを移動
3. インポートパスを更新

### ステップ3: 統合・テスト（1時間）

1. servicesとの統合を確認
2. 既存テストを移動・更新
3. UI動作確認

---

## 対象ファイル

- `src/features/inventory/components/ItemInventoryUI.ts` - 移動・作成
- `src/features/inventory/components/MaterialView.ts` - 移動・作成
- `src/features/inventory/components/InventoryList.ts` - 移動・作成
- `src/features/inventory/components/MaterialDetail.ts` - 移動・作成
- `src/features/inventory/components/index.ts` - 新規作成

---

## 受け入れ条件

- [ ] ItemInventoryUIが`features/inventory/components/`に存在する
- [ ] MaterialViewが`features/inventory/components/`に存在する
- [ ] コンポーネントがservicesを正しく使用している
- [ ] 既存のUI動作が維持されている
- [ ] 全テストがパスする
- [ ] `pnpm lint`がエラーなしで完了する

---

## 関連ドキュメント

- **TASK-0067**: shared/ui-components基盤作成
- **TASK-0085**: features/inventory/services作成
- **TASK-0087**: features/inventory/index.ts公開API作成
- **設計文書**: `.claude/rules/architecture.md`

---

## 信頼性レベルサマリー

### 項目別信頼性

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 機能要件 | 5 | 0 | 0 | 5 |
| テストケース | 0 | 0 | 0 | 0 |

### 全体評価

- **総項目数**: 5項目
- 🔵 **青信号**: 5項目 (100%) - 既存実装に基づく
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 良好（既存実装の移動が中心のため信頼性が高い）

---

## 変更履歴

| 日付 | 変更内容 |
|------|---------|
| 2026-02-09 | 初版作成 |

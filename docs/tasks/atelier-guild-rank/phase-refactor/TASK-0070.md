# TASK-0070: features/deck/components作成

## 基本情報

| 項目 | 値 |
|------|-----|
| **タスクID** | TASK-0070 |
| **タスク名** | features/deck/components作成 |
| **見積時間** | 4時間 |
| **依存タスク** | TASK-0067, TASK-0069 |
| **開発タイプ** | TDD |
| **フェーズ** | Phase 3 - features/deck機能 |
| **状態** | 🔲 未着手 |

---

## 概要

### 背景

デッキ機能に関連するUIコンポーネントを`features/deck/components/`に集約する必要がある。共通のBaseComponentを継承しつつ、デッキ固有のUI実装を機能単位で管理する。

### 目的

デッキ関連UIコンポーネントを`src/features/deck/components/`に移動し、shared/componentsのBaseComponentを継承する構造にする。

### 現状

- CardUI: `src/presentation/ui/components/CardUI.ts`
- HandDisplay: `src/presentation/ui/components/HandDisplay.ts`
- DraggableCardUI: `src/presentation/ui/components/DraggableCardUI.ts`

---

## 要件

### 機能要件

| 要件ID | 要件 | 信頼性 |
|--------|------|--------|
| REQ-070-01 | CardUIをfeatures/deck/components/に移動する | 🔵 |
| REQ-070-02 | HandDisplayをfeatures/deck/components/に移動する | 🔵 |
| REQ-070-03 | DraggableCardUIをfeatures/deck/components/に移動する | 🔵 |
| REQ-070-04 | BaseComponentを継承する構造に更新する | 🔵 |
| REQ-070-05 | インポートパスを@features/deck/componentsに更新する | 🔵 |

### 非機能要件

| 要件ID | 要件 |
|--------|------|
| NFR-070-01 | コンポーネントのテストがパスすること |
| NFR-070-02 | UIの見た目・動作に変化がないこと |
| NFR-070-03 | 循環参照が発生しないこと |

---

## テストケース

### コンポーネント移行テスト

```typescript
describe('features/deck/components', () => {
  describe('CardUI', () => {
    it('@features/deck/componentsからCardUIがインポートできること', () => {});
    it('BaseComponentを継承していること', () => {});
    it('カード表示が正常に動作すること', () => {});
  });

  describe('HandDisplay', () => {
    it('手札表示が正常に動作すること', () => {});
    it('カードの追加・削除が反映されること', () => {});
  });

  describe('DraggableCardUI', () => {
    it('ドラッグ操作が正常に動作すること', () => {});
    it('ドロップイベントが発火すること', () => {});
  });
});
```

---

## 実装計画

### ステップ1: ディレクトリ構造作成（30分）

1. src/features/deck/components/作成
2. index.tsファイル作成

### ステップ2: コンポーネント移行（2時間）

1. CardUIファイルを新ディレクトリにコピー
2. HandDisplayファイルを新ディレクトリにコピー
3. DraggableCardUIファイルを新ディレクトリにコピー
4. BaseComponent継承構造に更新

### ステップ3: インポートパス更新（1時間）

1. 旧パスからの参照を@features/deck/componentsに更新
2. 旧ファイルから再エクスポート（後方互換性）

### ステップ4: テスト実行・確認（30分）

1. 既存テストの実行
2. UIの視覚的確認
3. 全テストパスの確認

---

## 移行後のディレクトリ構造

```
src/features/deck/
├── types/
│   └── ...
├── services/
│   └── ...
├── components/
│   ├── index.ts
│   ├── CardUI.ts
│   ├── HandDisplay.ts
│   └── DraggableCardUI.ts
└── index.ts
```

---

## 対象ファイル

### 移行元

- `src/presentation/ui/components/CardUI.ts` → `src/features/deck/components/CardUI.ts`
- `src/presentation/ui/components/HandDisplay.ts` → `src/features/deck/components/HandDisplay.ts`
- `src/presentation/ui/components/DraggableCardUI.ts` → `src/features/deck/components/DraggableCardUI.ts`

### 更新対象

- デッキ関連コンポーネントを使用している全シーン
- tsconfig.jsonのパスエイリアス設定

---

## 受け入れ条件

- [ ] CardUIがsrc/features/deck/components/に移動されている
- [ ] HandDisplayがsrc/features/deck/components/に移動されている
- [ ] DraggableCardUIがsrc/features/deck/components/に移動されている
- [ ] BaseComponentを継承している
- [ ] @features/deck/componentsパスエイリアスでインポートできる
- [ ] UIの見た目・動作に変化がない
- [ ] 全テストがパスする
- [ ] `pnpm lint`がエラーなしで完了する

---

## 関連ドキュメント

- **TASK-0067**: shared/components作成（共通UIコンポーネント）
- **TASK-0069**: features/deck/services作成

---

## 信頼性レベルサマリー

### 項目別信頼性

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 機能要件 | 5 | 0 | 0 | 5 |
| テストケース | 0 | 0 | 0 | 0 |

### 全体評価

- **総項目数**: 5項目
- 🔵 **青信号**: 5項目 (100%) - 既存実装に基づく
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 良好（既存実装の移行が中心のため信頼性が高い）

---

## 変更履歴

| 日付 | 変更内容 |
|------|---------|
| 2026-02-09 | 初版作成 |

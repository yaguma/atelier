# TASK-0090: features/shop/components and index.ts作成

## 基本情報

| 項目 | 値 |
|------|-----|
| **タスクID** | TASK-0090 |
| **タスク名** | features/shop/components and index.ts作成 |
| **見積時間** | 4時間 |
| **依存タスク** | TASK-0067, TASK-0089 |
| **開発タイプ** | TDD |
| **フェーズ** | Phase 8 - features/shop機能 |
| **状態** | 🔲 未着手 |

---

## 概要

### 背景

ショップ関連のUIコンポーネントを`src/features/shop/components/`に集約し、公開APIを`src/features/shop/index.ts`に定義する必要がある。これにより、ショップ機能モジュールが完成する。

### 目的

ショップ関連UIコンポーネントを移動・整理し、機能レベルの公開APIを定義する。

### 現状

- UIコンポーネントがpresentation層に分散している可能性
- ショップ機能の公開APIが未定義

---

## 要件

### 機能要件

| 要件ID | 要件 | 信頼性 |
|--------|------|--------|
| REQ-090-01 | ShopUIコンポーネントを移動する | 🔵 |
| REQ-090-02 | ShopItemCardコンポーネントを移動する | 🔵 |
| REQ-090-03 | PurchaseConfirmDialogコンポーネントを作成する | 🔵 |
| REQ-090-04 | コンポーネントのエクスポートを整理する | 🔵 |
| REQ-090-05 | 機能レベルのindex.tsを作成する | 🔵 |

### 非機能要件

| 要件ID | 要件 |
|--------|------|
| NFR-090-01 | 既存のUI動作が維持されること |
| NFR-090-02 | コンポーネントがfeatures/shop/servicesを使用すること |
| NFR-090-03 | 外部から`@features/shop`でインポートできること |

---

## テストケース

### コンポーネントテスト

```typescript
describe('features/shop/components', () => {
  describe('ShopUI', () => {
    it('商品一覧が正しく表示されること', () => {});
    it('商品選択時にコールバックが呼ばれること', () => {});
    it('購入不可の商品が適切に表示されること', () => {});
  });

  describe('ShopItemCard', () => {
    it('商品情報が正しく表示されること', () => {});
    it('価格が正しく表示されること', () => {});
    it('在庫切れ時に適切な表示になること', () => {});
  });

  describe('PurchaseConfirmDialog', () => {
    it('確認ダイアログが表示されること', () => {});
    it('確定時にコールバックが呼ばれること', () => {});
    it('キャンセル時にコールバックが呼ばれること', () => {});
  });
});
```

### 公開APIテスト

```typescript
describe('features/shop/index.ts', () => {
  it('型が正しくエクスポートされること', () => {});
  it('サービス関数が正しくエクスポートされること', () => {});
  it('コンポーネントが正しくエクスポートされること', () => {});
});
```

---

## 実装計画

### ステップ1: コンポーネント移動（2時間）

1. 既存のショップ関連UIコンポーネントを特定
2. `src/features/shop/components/`に移動
3. インポートパスを更新

### ステップ2: 公開API作成（1時間）

1. `src/features/shop/index.ts`を作成
2. 型、サービス、コンポーネントをre-export
3. 内部実装の隠蔽を確認

### ステップ3: 統合・テスト（1時間）

1. 外部からのインポートテスト
2. 循環参照チェック
3. UI動作確認

---

## 対象ファイル

- `src/features/shop/components/ShopUI.ts` - 移動・作成
- `src/features/shop/components/ShopItemCard.ts` - 移動・作成
- `src/features/shop/components/PurchaseConfirmDialog.ts` - 新規作成
- `src/features/shop/components/index.ts` - 新規作成
- `src/features/shop/index.ts` - 新規作成

---

## 実装例

```typescript
// src/features/shop/index.ts

// 型のエクスポート
export type {
  ShopItem,
  ShopInventory,
  PurchaseResult,
  IShopService,
} from './types';

// サービス関数のエクスポート
export {
  canPurchase,
  calculatePrice,
  getAvailableItems,
  applyDiscount,
  executePurchase,
} from './services';

// コンポーネントのエクスポート
export {
  ShopUI,
  ShopItemCard,
  PurchaseConfirmDialog,
} from './components';
```

---

## 受け入れ条件

- [ ] ShopUIが`features/shop/components/`に存在する
- [ ] ShopItemCardが`features/shop/components/`に存在する
- [ ] PurchaseConfirmDialogが作成されている
- [ ] `src/features/shop/index.ts`が作成されている
- [ ] 外部から`@features/shop`でインポートできる
- [ ] 既存のUI動作が維持されている
- [ ] 全テストがパスする
- [ ] `pnpm lint`がエラーなしで完了する

---

## 関連ドキュメント

- **TASK-0067**: shared/ui-components基盤作成
- **TASK-0088**: features/shop/types作成
- **TASK-0089**: features/shop/services作成
- **設計文書**: `.claude/rules/architecture.md`

---

## 信頼性レベルサマリー

### 項目別信頼性

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 機能要件 | 5 | 0 | 0 | 5 |
| テストケース | 0 | 0 | 0 | 0 |

### 全体評価

- **総項目数**: 5項目
- 🔵 **青信号**: 5項目 (100%) - 既存実装に基づく
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 良好（既存実装の移動とバレルエクスポートが中心）

---

## 変更履歴

| 日付 | 変更内容 |
|------|---------|
| 2026-02-09 | 初版作成 |

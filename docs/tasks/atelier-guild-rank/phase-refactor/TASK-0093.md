# TASK-0093: features/rank/components and index.ts作成

## 基本情報

| 項目 | 値 |
|------|-----|
| **タスクID** | TASK-0093 |
| **タスク名** | features/rank/components and index.ts作成 |
| **見積時間** | 4時間 |
| **依存タスク** | TASK-0067, TASK-0092 |
| **開発タイプ** | TDD |
| **フェーズ** | Phase 9 - features/rank機能 |
| **状態** | 🔲 未着手 |

---

## 概要

### 背景

ランク関連のUIコンポーネントを`src/features/rank/components/`に集約し、公開APIを`src/features/rank/index.ts`に定義する必要がある。特にRankUpScene関連のコンポーネントの移動が重要である。

### 目的

ランク関連UIコンポーネント（特にRankUpScene関連）を移動・整理し、機能レベルの公開APIを定義する。

### 現状

- RankUpSceneがpresentation層に存在
- ランク表示関連のコンポーネントが分散している可能性

---

## 要件

### 機能要件

| 要件ID | 要件 | 信頼性 |
|--------|------|--------|
| REQ-093-01 | RankProgressBarコンポーネントを移動する | 🔵 |
| REQ-093-02 | RankBadgeコンポーネントを移動する | 🔵 |
| REQ-093-03 | PromotionDialogコンポーネントを作成する | 🔵 |
| REQ-093-04 | SpecialRuleDisplayコンポーネントを作成する | 🔵 |
| REQ-093-05 | 機能レベルのindex.tsを作成する | 🔵 |

### 非機能要件

| 要件ID | 要件 |
|--------|------|
| NFR-093-01 | 既存のUI動作が維持されること |
| NFR-093-02 | コンポーネントがfeatures/rank/servicesを使用すること |
| NFR-093-03 | 外部から`@features/rank`でインポートできること |

---

## テストケース

### コンポーネントテスト

```typescript
describe('features/rank/components', () => {
  describe('RankProgressBar', () => {
    it('進捗バーが正しく表示されること', () => {});
    it('進捗率に応じた幅になること', () => {});
    it('ランクに応じた色が適用されること', () => {});
  });

  describe('RankBadge', () => {
    it('ランクバッジが正しく表示されること', () => {});
    it('ランクに応じたアイコンが表示されること', () => {});
    it('ランク名が正しく表示されること', () => {});
  });

  describe('PromotionDialog', () => {
    it('昇格ダイアログが表示されること', () => {});
    it('新しいランク情報が表示されること', () => {});
    it('特殊ルール解放が表示されること', () => {});
  });

  describe('SpecialRuleDisplay', () => {
    it('特殊ルール一覧が表示されること', () => {});
    it('有効なルールがハイライトされること', () => {});
    it('ルールの説明が表示されること', () => {});
  });
});
```

### 公開APIテスト

```typescript
describe('features/rank/index.ts', () => {
  it('型が正しくエクスポートされること', () => {});
  it('サービス関数が正しくエクスポートされること', () => {});
  it('コンポーネントが正しくエクスポートされること', () => {});
});
```

---

## 実装計画

### ステップ1: コンポーネント移動（2時間）

1. 既存のランク関連UIコンポーネントを特定
2. `src/features/rank/components/`に移動
3. RankUpScene関連コンポーネントを抽出

### ステップ2: 公開API作成（1時間）

1. `src/features/rank/index.ts`を作成
2. 型、サービス、コンポーネントをre-export
3. 内部実装の隠蔽を確認

### ステップ3: 統合・テスト（1時間）

1. 外部からのインポートテスト
2. RankUpSceneとの統合確認
3. UI動作確認

---

## 対象ファイル

- `src/features/rank/components/RankProgressBar.ts` - 移動・作成
- `src/features/rank/components/RankBadge.ts` - 移動・作成
- `src/features/rank/components/PromotionDialog.ts` - 新規作成
- `src/features/rank/components/SpecialRuleDisplay.ts` - 新規作成
- `src/features/rank/components/index.ts` - 新規作成
- `src/features/rank/index.ts` - 新規作成

---

## 実装例

```typescript
// src/features/rank/index.ts

// 型のエクスポート
export type {
  GuildRank,
  SpecialRule,
  RankProgress,
  IRankService,
} from './types';

// サービス関数のエクスポート
export {
  calculateRankProgress,
  checkPromotion,
  getNextRankRequirements,
  calculateContribution,
  applySpecialRule,
} from './services';

// コンポーネントのエクスポート
export {
  RankProgressBar,
  RankBadge,
  PromotionDialog,
  SpecialRuleDisplay,
} from './components';
```

---

## 受け入れ条件

- [ ] RankProgressBarが`features/rank/components/`に存在する
- [ ] RankBadgeが`features/rank/components/`に存在する
- [ ] PromotionDialogが作成されている
- [ ] SpecialRuleDisplayが作成されている
- [ ] `src/features/rank/index.ts`が作成されている
- [ ] 外部から`@features/rank`でインポートできる
- [ ] RankUpSceneとの統合が維持されている
- [ ] 全テストがパスする
- [ ] `pnpm lint`がエラーなしで完了する

---

## 関連ドキュメント

- **TASK-0067**: shared/ui-components基盤作成
- **TASK-0091**: features/rank/types作成
- **TASK-0092**: features/rank/services作成
- **設計文書**: `.claude/rules/architecture.md`

---

## 信頼性レベルサマリー

### 項目別信頼性

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 機能要件 | 5 | 0 | 0 | 5 |
| テストケース | 0 | 0 | 0 | 0 |

### 全体評価

- **総項目数**: 5項目
- 🔵 **青信号**: 5項目 (100%) - 既存実装に基づく
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 良好（既存実装の移動とバレルエクスポートが中心）

---

## 変更履歴

| 日付 | 変更内容 |
|------|---------|
| 2026-02-09 | 初版作成 |

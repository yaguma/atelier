# TASK-0062: tsconfigパスエイリアス設定更新

**タスクID**: TASK-0062
**タスクタイプ**: DIRECT
**推定工数**: 4時間
**フェーズ**: Phase 1 - 基盤構築
**信頼性レベル**: 🔵 *.claude/rules/architecture.mdに基づく*

## 関連文書

- **概要**: [📋 overview.md](../overview.md)
- **設計文書**: [📐 architecture.md](../../design/atelier-guild-rank/architecture-overview.md)

## タスク概要

新しいパスエイリアス（@features, @shared, @scenes）を設定する。tsconfig.json、vite.config.ts、vitest.config.ts の3つの設定ファイルを更新し、新しいディレクトリ構造に対応したパス解決を可能にする。

## 依存タスク

- **前提タスク**: [TASK-0061](TASK-0061.md)
- **後続タスク**: なし

## 完了条件

- [ ] tsconfig.json の paths に新しいエイリアスが追加されている
- [ ] vite.config.ts の resolve.alias に新しいエイリアスが追加されている
- [ ] vitest.config.ts の alias に新しいエイリアスが追加されている
- [ ] パスエイリアスが正しく解決されることを確認
- [ ] TypeScriptコンパイルが成功する
- [ ] ビルドが成功する

---

## 主要実装項目

### 1. tsconfig.json の paths 更新 🔵

**信頼性**: 🔵 *.claude/rules/architecture.md - パスエイリアス仕様*

新しいパスエイリアスを追加する。

**実装ファイル**: `atelier-guild-rank/tsconfig.json`

```json
{
  "compilerOptions": {
    "paths": {
      "@features/*": ["src/features/*"],
      "@shared/*": ["src/shared/*"],
      "@scenes/*": ["src/scenes/*"]
    }
  }
}
```

### 2. vite.config.ts の resolve.alias 更新 🔵

**信頼性**: 🔵 *.claude/rules/architecture.md - ビルド設定仕様*

Viteビルド時のパス解決設定を追加する。

**実装ファイル**: `atelier-guild-rank/vite.config.ts`

```typescript
import path from 'path';

export default defineConfig({
  resolve: {
    alias: {
      '@features': path.resolve(__dirname, './src/features'),
      '@shared': path.resolve(__dirname, './src/shared'),
      '@scenes': path.resolve(__dirname, './src/scenes'),
    },
  },
});
```

### 3. vitest.config.ts の alias 更新 🔵

**信頼性**: 🔵 *.claude/rules/architecture.md - テスト設定仕様*

Vitestテスト実行時のパス解決設定を追加する。

**実装ファイル**: `atelier-guild-rank/vitest.config.ts`

```typescript
export default defineConfig({
  test: {
    alias: {
      '@features': path.resolve(__dirname, './src/features'),
      '@shared': path.resolve(__dirname, './src/shared'),
      '@scenes': path.resolve(__dirname, './src/scenes'),
    },
  },
});
```

---

## 基本的なテスト要件

### パスエイリアス解決確認 🔵

**信頼性**: 🔵 *TypeScriptコンパイラで検証*

- TypeScriptコンパイルが成功すること（`pnpm tsc --noEmit`）
- 新しいエイリアスでインポートが解決されること

### ビルド確認 🔵

**信頼性**: 🔵 *ビルドプロセスで検証*

- `pnpm build` が成功すること
- バンドルにパスエイリアスの解決エラーがないこと

### テスト実行確認 🔵

**信頼性**: 🔵 *テストランナーで検証*

- `pnpm test` が成功すること
- テストファイル内でパスエイリアスが解決されること

---

## 実装手順

### DIRECTタスクの場合

1. `/direct-setup` - 直接実装・設定
2. `/direct-verify` - 動作確認・品質確認

---

## 信頼性レベルサマリー

- **総項目数**: 6項目
- 🔵 **青信号**: 6項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質（.claude/rules/architecture.mdに明確な仕様あり）

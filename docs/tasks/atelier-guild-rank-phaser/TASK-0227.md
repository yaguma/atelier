# TASK-0227: AlchemyContainer設計

**タスクID**: TASK-0227
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 3 - フェーズコンテナ・メイン画面実装
**信頼性レベル**: 🔵 *設計書に記載*

## 関連文書

- **概要**: [overview.md](./overview.md)
- **UI設計**: [ui-design/overview.md](../../design/atelier-guild-rank-phaser/ui-design/overview.md)

## タスク概要

調合フェーズコンテナの基本設計を行う。レシピ選択、素材選択、調合実行のインターフェースを定義する。

## 依存タスク

- **前提タスク**: TASK-0213, TASK-0195, TASK-0226
- **後続タスク**: TASK-0228

## 完了条件

- [ ] IAlchemyContainerインターフェースが定義されている
- [ ] AlchemyContainerOptionsが定義されている
- [ ] 基本レイアウト（手札、素材選択、プレビュー）が配置されている
- [ ] BasePhaseContainerを継承している

---

## 実装詳細

### 1. AlchemyContainerレイアウト定数 🔵

**信頼性**: 🔵 *UI設計書*

**実装ファイル**: `src/presentation/phaser/ui/phase/AlchemyContainerConstants.ts`

```typescript
export const AlchemyContainerLayout = {
  // 全体サイズ
  WIDTH: 800,
  HEIGHT: 550,
  PADDING: 20,

  // 手札エリア（レシピカード）
  HAND_AREA: {
    X: 20,
    Y: 60,
    WIDTH: 500,
    HEIGHT: 180,
  },

  // 素材選択エリア
  MATERIAL_AREA: {
    X: 20,
    Y: 260,
    WIDTH: 500,
    HEIGHT: 180,
  },

  // プレビューパネル
  PREVIEW_PANEL: {
    X: 540,
    Y: 60,
    WIDTH: 240,
    HEIGHT: 380,
  },

  // アクションエリア
  ACTION_AREA: {
    Y: 480,
    BUTTON_SPACING: 20,
  },
} as const;
```

### 2. IAlchemyContainerインターフェース 🔵

**信頼性**: 🔵 *フェーズコンテナ設計*

**実装ファイル**: `src/presentation/phaser/ui/phase/IAlchemyContainer.ts`

```typescript
import { RecipeCard } from '../../../../domain/card/RecipeCard';
import { Material } from '../../../../domain/material/Material';
import { Item } from '../../../../domain/item/Item';
import { IPhaseContainer } from './IPhaseContainer';

export interface AlchemyResult {
  recipe: RecipeCard;
  usedMaterials: Material[];
  craftedItem: Item;
  quality: string;
  traits: string[];
}

export interface AlchemyContainerOptions {
  scene: Phaser.Scene;
  eventBus: EventBus;
  x?: number;
  y?: number;
  onAlchemyComplete?: (result: AlchemyResult) => void;
  onSkip?: () => void;
}

export interface IAlchemyContainer extends IPhaseContainer {
  // レシピカード設定
  setRecipeCards(cards: RecipeCard[]): void;
  getSelectedRecipe(): RecipeCard | null;

  // 素材設定
  setAvailableMaterials(materials: Material[]): void;
  getSelectedMaterials(): Material[];

  // 操作
  selectRecipe(card: RecipeCard): void;
  selectMaterial(material: Material): void;
  deselectMaterial(material: Material): void;
  clearMaterials(): void;

  // 調合
  canCraft(): boolean;
  craft(): void;
}
```

### 3. AlchemyContainer基本実装 🔵

**信頼性**: 🔵 *調合フェーズコンテナ*

**実装ファイル**: `src/presentation/phaser/ui/phase/AlchemyContainer.ts`

```typescript
import Phaser from 'phaser';
import { BasePhaseContainer } from './BasePhaseContainer';
import { IAlchemyContainer, AlchemyContainerOptions, AlchemyResult } from './IAlchemyContainer';
import { AlchemyContainerLayout } from './AlchemyContainerConstants';
import { RecipeCard } from '../../../../domain/card/RecipeCard';
import { Material } from '../../../../domain/material/Material';
import { Item } from '../../../../domain/item/Item';
import { HandContainer } from '../hand/HandContainer';
import { MaterialOptionView } from '../material/MaterialOptionView';
import { AlchemyPreviewPanel } from '../alchemy/AlchemyPreviewPanel';
import { EventBus } from '../../core/EventBus';
import { Colors } from '../../config/ColorPalette';
import { TextStyles } from '../../config/TextStyles';

export class AlchemyContainer extends BasePhaseContainer implements IAlchemyContainer {
  private recipeCards: RecipeCard[] = [];
  private selectedRecipe: RecipeCard | null = null;
  private availableMaterials: Material[] = [];
  private selectedMaterials: Material[] = [];

  // UI Components
  private handContainer?: HandContainer;
  private materialView?: MaterialOptionView;
  private previewPanel?: AlchemyPreviewPanel;

  // Buttons
  private craftButton?: Phaser.GameObjects.Container;
  private skipButton?: Phaser.GameObjects.Container;

  private onAlchemyComplete?: (result: AlchemyResult) => void;
  private onSkip?: () => void;

  constructor(options: AlchemyContainerOptions) {
    super({
      scene: options.scene,
      eventBus: options.eventBus,
      x: options.x ?? 0,
      y: options.y ?? 0,
      width: AlchemyContainerLayout.WIDTH,
      height: AlchemyContainerLayout.HEIGHT,
    });

    this.onAlchemyComplete = options.onAlchemyComplete;
    this.onSkip = options.onSkip;

    this.createBackground();
    this.createTitle('⚗️ 調合フェーズ');
    this.createLayout();
    this.createActionButtons();
  }

  private createLayout(): void {
    const { HAND_AREA, MATERIAL_AREA, PREVIEW_PANEL } = AlchemyContainerLayout;

    // 手札エリアラベル
    this.createAreaLabel(HAND_AREA.X, HAND_AREA.Y - 20, '📋 レシピを選択');

    // 手札コンテナ
    this.handContainer = new HandContainer(this.scene, {
      x: HAND_AREA.X,
      y: HAND_AREA.Y,
      maxCards: 5,
      cardWidth: 90,
      cardHeight: 140,
      layout: 'horizontal',
      onCardSelect: (card) => this.handleRecipeSelect(card as RecipeCard),
    });
    this.container.add(this.handContainer.container);

    // 素材選択エリアラベル
    this.createAreaLabel(MATERIAL_AREA.X, MATERIAL_AREA.Y - 20, '🧪 素材を選択');

    // プレビューパネル
    this.previewPanel = new AlchemyPreviewPanel(this.scene, {
      x: PREVIEW_PANEL.X,
      y: PREVIEW_PANEL.Y,
    });
    this.container.add(this.previewPanel.container);
  }

  private createAreaLabel(x: number, y: number, text: string): void {
    const label = this.scene.add.text(x, y, text, {
      ...TextStyles.body,
      fontSize: '13px',
      color: '#aaaaaa',
    });
    this.container.add(label);
  }

  private createActionButtons(): void {
    const { ACTION_AREA, WIDTH } = AlchemyContainerLayout;
    const centerX = WIDTH / 2;

    // 調合ボタン
    this.craftButton = this.createButton(
      centerX + 60,
      ACTION_AREA.Y,
      '⚗️ 調合する',
      () => this.craft(),
      true
    );
    this.container.add(this.craftButton);

    // スキップボタン
    this.skipButton = this.createButton(
      centerX - 60,
      ACTION_AREA.Y,
      'スキップ',
      () => this.handleSkip(),
      false
    );
    this.container.add(this.skipButton);

    // 素材クリアボタン
    const clearButton = this.createButton(
      WIDTH - 80,
      ACTION_AREA.Y,
      '🔄 リセット',
      () => this.clearMaterials(),
      false
    );
    this.container.add(clearButton);
  }

  setRecipeCards(cards: RecipeCard[]): void {
    this.recipeCards = cards;
    this.handContainer?.setCards(cards);
    this.selectedRecipe = null;
    this.updatePreview();
  }

  getSelectedRecipe(): RecipeCard | null {
    return this.selectedRecipe;
  }

  private handleRecipeSelect(card: RecipeCard): void {
    this.selectedRecipe = card;
    this.clearMaterials();
    this.updateMaterialOptions();
    this.updatePreview();
    this.eventBus.emit('alchemy:recipe:selected', { recipe: card });
  }

  selectRecipe(card: RecipeCard): void {
    this.handleRecipeSelect(card);
    this.handContainer?.selectCard(card);
  }

  setAvailableMaterials(materials: Material[]): void {
    this.availableMaterials = materials;
    this.updateMaterialOptions();
  }

  private updateMaterialOptions(): void {
    // 既存のビューを破棄
    if (this.materialView) {
      this.materialView.destroy();
    }

    if (!this.selectedRecipe) {
      // レシピ未選択時は空
      return;
    }

    const { MATERIAL_AREA } = AlchemyContainerLayout;

    // レシピに合った素材のみフィルタ
    const filteredMaterials = this.filterMaterialsForRecipe(this.availableMaterials, this.selectedRecipe);

    this.materialView = new MaterialOptionView(this.scene, {
      x: MATERIAL_AREA.X,
      y: MATERIAL_AREA.Y,
      options: filteredMaterials.map(m => ({
        material: m,
        quantity: 1, // インベントリから取得すべき
      })),
      maxSelections: this.selectedRecipe.requiredMaterialCount ?? 3,
      onSelect: (material) => this.handleMaterialSelect(material),
      onDeselect: (material) => this.handleMaterialDeselect(material),
    });
    this.container.add(this.materialView.container);
  }

  private filterMaterialsForRecipe(materials: Material[], recipe: RecipeCard): Material[] {
    // レシピの要求カテゴリに合う素材をフィルタ
    if (!recipe.requiredCategories || recipe.requiredCategories.length === 0) {
      return materials;
    }
    return materials.filter(m =>
      recipe.requiredCategories!.includes(m.category)
    );
  }

  getSelectedMaterials(): Material[] {
    return [...this.selectedMaterials];
  }

  selectMaterial(material: Material): void {
    if (!this.selectedMaterials.includes(material)) {
      this.selectedMaterials.push(material);
      this.materialView?.selectMaterial(material);
      this.updatePreview();
    }
  }

  deselectMaterial(material: Material): void {
    this.selectedMaterials = this.selectedMaterials.filter(m => m !== material);
    this.materialView?.deselectMaterial(material);
    this.updatePreview();
  }

  clearMaterials(): void {
    this.selectedMaterials = [];
    this.materialView?.clearSelection();
    this.updatePreview();
    this.eventBus.emit('alchemy:materials:cleared', {});
  }

  private handleMaterialSelect(material: Material): void {
    this.selectedMaterials.push(material);
    this.updatePreview();
    this.eventBus.emit('alchemy:material:selected', { material });
  }

  private handleMaterialDeselect(material: Material): void {
    this.selectedMaterials = this.selectedMaterials.filter(m => m !== material);
    this.updatePreview();
    this.eventBus.emit('alchemy:material:deselected', { material });
  }

  private updatePreview(): void {
    if (!this.selectedRecipe) {
      this.previewPanel?.setPreview(null);
      this.updateCraftButtonState();
      return;
    }

    // 品質予測（簡易版）
    const predictedQuality = this.predictQuality(this.selectedMaterials);
    const predictedTraits = this.predictTraits(this.selectedMaterials);
    const missingMaterials = this.getMissingMaterials();

    this.previewPanel?.setPreview({
      recipe: this.selectedRecipe,
      materials: this.selectedMaterials,
      predictedQuality,
      predictedTraits,
      canCraft: this.canCraft(),
      missingMaterials,
    });

    this.updateCraftButtonState();
  }

  private predictQuality(materials: Material[]): string {
    if (materials.length === 0) return 'normal';

    // 素材の品質から予測（簡易版）
    const qualityScores: Record<string, number> = {
      'legendary': 5,
      'epic': 4,
      'rare': 3,
      'good': 2,
      'normal': 1,
      'poor': 0,
    };

    const avgScore = materials.reduce((sum, m) =>
      sum + (qualityScores[m.quality ?? 'normal'] ?? 1), 0
    ) / materials.length;

    if (avgScore >= 4.5) return 'legendary';
    if (avgScore >= 3.5) return 'epic';
    if (avgScore >= 2.5) return 'rare';
    if (avgScore >= 1.5) return 'good';
    return 'normal';
  }

  private predictTraits(materials: Material[]): string[] {
    // 素材の特性から継承（簡易版）
    const traits: string[] = [];
    materials.forEach(m => {
      if (m.traits) {
        traits.push(...m.traits);
      }
    });
    // 重複を除去して最大3つ
    return [...new Set(traits)].slice(0, 3);
  }

  private getMissingMaterials(): string[] {
    if (!this.selectedRecipe) return [];

    const requiredCount = this.selectedRecipe.requiredMaterialCount ?? 1;
    if (this.selectedMaterials.length < requiredCount) {
      return [`素材が${requiredCount - this.selectedMaterials.length}個不足`];
    }
    return [];
  }

  canCraft(): boolean {
    if (!this.selectedRecipe) return false;

    const requiredCount = this.selectedRecipe.requiredMaterialCount ?? 1;
    return this.selectedMaterials.length >= requiredCount;
  }

  private updateCraftButtonState(): void {
    if (this.craftButton) {
      this.craftButton.setAlpha(this.canCraft() ? 1 : 0.5);
    }
  }

  async craft(): Promise<void> {
    if (!this.canCraft()) {
      this.showCannotCraftFeedback();
      return;
    }

    // 操作を無効化
    this.setEnabled(false);

    // 調合アニメーション
    await this.playCraftAnimation();

    // 結果生成
    const result = this.generateCraftResult();

    // イベント発火
    this.eventBus.emit('alchemy:craft', result);

    // 成功表示
    await this.showCraftSuccess(result);

    // コールバック
    if (this.onAlchemyComplete) {
      this.onAlchemyComplete(result);
    }
  }

  private showCannotCraftFeedback(): void {
    if (this.craftButton) {
      this.scene.tweens.add({
        targets: this.craftButton,
        x: this.craftButton.x + 5,
        duration: 50,
        yoyo: true,
        repeat: 3,
      });
    }
  }

  private async playCraftAnimation(): Promise<void> {
    // 調合アニメーション（TASK-0229で詳細実装）
    return new Promise(resolve => {
      this.scene.time.delayedCall(1000, resolve);
    });
  }

  private generateCraftResult(): AlchemyResult {
    return {
      recipe: this.selectedRecipe!,
      usedMaterials: this.selectedMaterials,
      craftedItem: this.createCraftedItem(),
      quality: this.predictQuality(this.selectedMaterials),
      traits: this.predictTraits(this.selectedMaterials),
    };
  }

  private createCraftedItem(): Item {
    // 調合結果アイテム生成（Domain層で行うべき）
    return {
      id: `item-${Date.now()}`,
      name: this.selectedRecipe?.name ?? 'Unknown Item',
      quality: this.predictQuality(this.selectedMaterials),
      traits: this.predictTraits(this.selectedMaterials),
    } as Item;
  }

  private async showCraftSuccess(result: AlchemyResult): Promise<void> {
    // 成功表示（TASK-0229で詳細実装）
    return new Promise(resolve => {
      this.scene.time.delayedCall(1000, resolve);
    });
  }

  private handleSkip(): void {
    this.eventBus.emit('alchemy:skip', {});
    if (this.onSkip) {
      this.onSkip();
    }
  }

  private setEnabled(enabled: boolean): void {
    this.handContainer?.setEnabled(enabled);
    this.materialView?.setEnabled(enabled);
    if (this.craftButton) {
      this.craftButton.setAlpha(enabled ? 1 : 0.5);
    }
    if (this.skipButton) {
      this.skipButton.setAlpha(enabled ? 1 : 0.5);
    }
  }

  enter(): void {
    super.enter();
    this.selectedRecipe = null;
    this.clearMaterials();
    this.updatePreview();
  }

  exit(): void {
    super.exit();
  }

  destroy(): void {
    this.handContainer?.destroy();
    this.materialView?.destroy();
    this.previewPanel?.destroy();
    super.destroy();
  }
}
```

---

## 単体テスト要件

### テストケース1: コンテナ初期化 🔵

**Given**: AlchemyContainerを初期化
**When**: オプションを渡してインスタンス化
**Then**: コンテナが作成される

### テストケース2: レシピカード設定 🔵

**Given**: AlchemyContainerがある
**When**: setRecipeCards(cards)を呼ぶ
**Then**: 手札にレシピカードが表示される

### テストケース3: 素材設定 🔵

**Given**: レシピが選択されている
**When**: setAvailableMaterials(materials)を呼ぶ
**Then**: 選択可能な素材が表示される

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-0227` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- HandContainerとMaterialOptionViewの統合
- AlchemyPreviewPanelのリアルタイム更新
- 品質予測ロジックはDomain層に委譲すべき

---

## 信頼性レベルサマリー

- **総項目数**: 3項目
- 🔵 **青信号**: 3項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質

# TASK-0186: TitleScene新規ゲームボタン実装

**タスクID**: TASK-0186
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 2 - 基本シーン・共通UI実装
**信頼性レベル**: 🔵 *設計書に明確な記載あり*

## 関連文書

- **概要**: [overview.md](./overview.md)
- **UI設計**: [ui-design/overview.md](../../design/atelier-guild-rank-phaser/ui-design/overview.md)
- **データフロー**: [dataflow.md](../../design/atelier-guild-rank-phaser/dataflow.md)

## タスク概要

タイトル画面の「新規ゲーム」ボタンを実装する。

## 依存タスク

- **前提タスク**: TASK-0185
- **後続タスク**: TASK-0187

## 完了条件

- [ ] 新規ゲームボタンが表示される
- [ ] ボタンクリックでイベントが発行される
- [ ] クリック後にMainSceneへ遷移する
- [ ] ホバーエフェクトが動作する

---

## 実装詳細

### 1. 新規ゲームボタン実装 🔵

**信頼性**: 🔵 *フローが設計書に記載*

**実装ファイル**: `src/presentation/phaser/scenes/TitleScene.ts` に追加

```typescript
import { UIActionEvents } from '../events/EventTypes';
import { SceneManager } from '../managers/SceneManager';
import Label from 'phaser3-rex-plugins/templates/ui/label/Label';

// TitleSceneクラス内

private newGameButton!: Label;

private createMenuButtons(): void {
  // 新規ゲームボタン
  this.newGameButton = this.uiFactory.createButton({
    x: 0,
    y: 0,
    text: '新規ゲーム',
    width: 200,
    height: 50,
    onClick: () => this.onNewGameClick(),
  });
  this.menuContainer.add(this.newGameButton);

  // コンティニューボタンはTASK-0187で追加
}

private onNewGameClick(): void {
  // SE再生
  if (this.cache.audio.exists(AudioKeys.SE_CLICK)) {
    this.sound.play(AudioKeys.SE_CLICK);
  }

  // BGM停止
  this.sound.stopAll();

  // イベント発行
  this.eventBus.emitVoid(UIActionEvents.NEW_GAME_CLICKED);

  // MainSceneへ遷移
  SceneManager.getInstance().goTo(SceneKeys.MAIN, {
    isNewGame: true,
  });
}
```

### 2. イベントフロー 🔵

**信頼性**: 🔵 *dataflow.mdに記載*

```
[新規ゲームボタン]
    ↓ クリック
[TitleScene.onNewGameClick()]
    ↓ EventBus.emit('ui:newGame:clicked')
[Application層で受信]
    ↓ ゲーム初期化処理
[SceneManager.goTo('MainScene')]
    ↓ シーン遷移
[MainScene]
```

---

## 単体テスト要件

### テストケース1: ボタン表示 🔵

**Given**: TitleSceneが作成された
**When**: メニューコンテナを確認する
**Then**: 新規ゲームボタンが存在する

### テストケース2: クリックイベント 🔵

**Given**: 新規ゲームボタンがある
**When**: ボタンをクリックする
**Then**: NEW_GAME_CLICKEDイベントが発行される

### テストケース3: シーン遷移 🔵

**Given**: 新規ゲームボタンがある
**When**: ボタンをクリックする
**Then**: MainSceneに遷移する

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-0186` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- 連打防止の実装を検討
- 遷移中のボタン無効化
- SEがない場合のエラー回避

---

## 信頼性レベルサマリー

- **総項目数**: 2項目
- 🔵 **青信号**: 2項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質

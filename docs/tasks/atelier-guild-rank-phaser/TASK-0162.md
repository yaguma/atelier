# TASK-0162: TextStyles定義

**タスクID**: TASK-0162
**タスクタイプ**: DIRECT
**推定工数**: 4時間
**フェーズ**: Phase 1 - Phaser基盤・インフラ構築
**信頼性レベル**: 🟡 *妥当な推測に基づく*

## 関連文書

- **概要**: [overview.md](./overview.md)
- **要件定義**: [requirements.md](../../spec/atelier-guild-rank/requirements.md)
- **UI設計**: [ui-design/overview.md](../../design/atelier-guild-rank-phaser/ui-design/overview.md)

## タスク概要

ゲーム内で使用するテキストスタイル（フォント、サイズ、色）を定義する。

## 依存タスク

- **前提タスク**: TASK-0160
- **後続タスク**: TASK-0189, TASK-0199

## 完了条件

- [x] タイトル用テキストスタイルが定義されている
- [x] 本文用テキストスタイルが定義されている
- [x] ラベル用テキストスタイルが定義されている
- [x] カード用テキストスタイルが定義されている
- [x] ボタン用テキストスタイルが定義されている

---

## 実装詳細

### 1. ベーステキストスタイル 🟡

**信頼性**: 🟡 *ui-design/overview.mdを参考*

**実装ファイル**: `src/presentation/phaser/config/TextStyles.ts`

```typescript
import Phaser from 'phaser';

// 基本フォント設定
const BASE_FONT = '"Noto Sans JP", "Hiragino Sans", sans-serif';

// テキストスタイルタイプ
export type TextStyleKey = keyof typeof TextStyles;

export const TextStyles = {
  // タイトル（大）
  titleLarge: {
    fontFamily: BASE_FONT,
    fontSize: '48px',
    fontStyle: 'bold',
    color: '#ffffff',
    stroke: '#000000',
    strokeThickness: 2,
  } as Phaser.Types.GameObjects.Text.TextStyle,

  // タイトル（中）
  titleMedium: {
    fontFamily: BASE_FONT,
    fontSize: '36px',
    fontStyle: 'bold',
    color: '#ffffff',
  } as Phaser.Types.GameObjects.Text.TextStyle,

  // タイトル（小）
  titleSmall: {
    fontFamily: BASE_FONT,
    fontSize: '24px',
    fontStyle: 'bold',
    color: '#ffffff',
  } as Phaser.Types.GameObjects.Text.TextStyle,

  // 本文
  body: {
    fontFamily: BASE_FONT,
    fontSize: '16px',
    color: '#e0e0e0',
  } as Phaser.Types.GameObjects.Text.TextStyle,

  // 本文（小）
  bodySmall: {
    fontFamily: BASE_FONT,
    fontSize: '14px',
    color: '#b0b0b0',
  } as Phaser.Types.GameObjects.Text.TextStyle,

  // ラベル
  label: {
    fontFamily: BASE_FONT,
    fontSize: '14px',
    fontStyle: 'bold',
    color: '#a0a0a0',
  } as Phaser.Types.GameObjects.Text.TextStyle,

  // 数値（大）
  numberLarge: {
    fontFamily: BASE_FONT,
    fontSize: '32px',
    fontStyle: 'bold',
    color: '#ffd700',
  } as Phaser.Types.GameObjects.Text.TextStyle,

  // 数値
  number: {
    fontFamily: BASE_FONT,
    fontSize: '20px',
    fontStyle: 'bold',
    color: '#ffffff',
  } as Phaser.Types.GameObjects.Text.TextStyle,

  // ボタン
  button: {
    fontFamily: BASE_FONT,
    fontSize: '18px',
    fontStyle: 'bold',
    color: '#ffffff',
  } as Phaser.Types.GameObjects.Text.TextStyle,

  // カード名
  cardName: {
    fontFamily: BASE_FONT,
    fontSize: '14px',
    fontStyle: 'bold',
    color: '#ffffff',
  } as Phaser.Types.GameObjects.Text.TextStyle,

  // カード説明
  cardDescription: {
    fontFamily: BASE_FONT,
    fontSize: '11px',
    color: '#c0c0c0',
    wordWrap: { width: 140 },
  } as Phaser.Types.GameObjects.Text.TextStyle,

  // カードコスト
  cardCost: {
    fontFamily: BASE_FONT,
    fontSize: '16px',
    fontStyle: 'bold',
    color: '#ffd700',
  } as Phaser.Types.GameObjects.Text.TextStyle,
} as const;

// ヘルパー関数
export const getTextStyle = (key: TextStyleKey): Phaser.Types.GameObjects.Text.TextStyle => {
  return { ...TextStyles[key] };
};

export const mergeTextStyle = (
  base: TextStyleKey,
  overrides: Partial<Phaser.Types.GameObjects.Text.TextStyle>
): Phaser.Types.GameObjects.Text.TextStyle => {
  return { ...TextStyles[base], ...overrides };
};
```

---

## 実装手順

1. `/tsumiki:direct-setup TASK-0162` - セットアップ実行
2. `/tsumiki:direct-verify` - 動作確認

---

## 注意事項

- 日本語フォントの読み込みを確認
- Webフォント使用時はプリロードが必要
- カラーコードはColorPalette（TASK-0163）と整合性を取る

---

## 信頼性レベルサマリー

- **総項目数**: 1項目
- 🔵 **青信号**: 0項目 (0%)
- 🟡 **黄信号**: 1項目 (100%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 中品質

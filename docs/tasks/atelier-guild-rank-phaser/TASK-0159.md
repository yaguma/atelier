# TASK-0159: Vite設定更新（Phaser対応）

**タスクID**: TASK-0159
**タスクタイプ**: DIRECT
**推定工数**: 4時間
**フェーズ**: Phase 1 - Phaser基盤・インフラ構築
**信頼性レベル**: 🔴 *Phaser固有の調査が必要*

## 関連文書

- **概要**: [overview.md](./overview.md)
- **要件定義**: [requirements.md](../../spec/atelier-guild-rank/requirements.md)
- **設計文書**: [architecture.md](../../design/atelier-guild-rank-phaser/architecture.md)

## タスク概要

ViteビルドシステムをPhaser対応に更新し、最適なバンドル設定を行う。

## 依存タスク

- **前提タスク**: TASK-0158
- **後続タスク**: TASK-0160

## 完了条件

- [x] vite.config.tsがPhaser対応に更新されている
- [x] 開発サーバーでPhaser含むビルドが動作する
- [x] 本番ビルドが正常に完了する
- [x] ソースマップが正しく生成される

---

## 実装詳細

### 1. vite.config.ts更新 🔴

**信頼性**: 🔴 *Phaser特有のバンドル設定が必要*

```typescript
import { defineConfig } from 'vite';

export default defineConfig({
  base: './',
  build: {
    rollupOptions: {
      output: {
        manualChunks: {
          phaser: ['phaser'],
          rexui: ['phaser3-rex-plugins']
        }
      }
    },
    chunkSizeWarningLimit: 2000, // Phaserは大きいため
  },
  optimizeDeps: {
    include: ['phaser', 'phaser3-rex-plugins']
  },
  server: {
    port: 3000
  }
});
```

### 2. アセットパス設定 🔴

**信頼性**: 🔴 *Phaser のアセットロード方式の調査が必要*

publicフォルダの構成:
```
public/
├── assets/
│   ├── images/
│   ├── audio/
│   └── fonts/
└── data/
    └── master/
```

### 3. 環境変数設定 🟡

**信頼性**: 🟡 *既存パターンを参考*

```typescript
// .env.development
VITE_DEBUG=true

// .env.production
VITE_DEBUG=false
```

---

## 実装手順

1. `/tsumiki:direct-setup TASK-0159` - セットアップ実行
2. `/tsumiki:direct-verify` - 動作確認

---

## 注意事項

- HMR（Hot Module Replacement）がPhaserと動作するか確認
- 既存のHTML版ビルドに影響がないことを確認
- バンドルサイズの計測を実施

---

## 信頼性レベルサマリー

- **総項目数**: 3項目
- 🔵 **青信号**: 0項目 (0%)
- 🟡 **黄信号**: 1項目 (33%)
- 🔴 **赤信号**: 2項目 (67%)

**品質評価**: 要調査

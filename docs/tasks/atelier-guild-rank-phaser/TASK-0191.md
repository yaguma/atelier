# TASK-0191: CardViewレシピカード実装

**タスクID**: TASK-0191
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 2 - 基本シーン・共通UI実装
**信頼性レベル**: 🟡 *妥当な推測に基づく*

## 関連文書

- **概要**: [overview.md](./overview.md)
- **UI設計**: [ui-design/overview.md](../../design/atelier-guild-rank-phaser/ui-design/overview.md)

## タスク概要

レシピカード（RecipeCard）の視覚的表現を実装する。

## 依存タスク

- **前提タスク**: TASK-0190
- **後続タスク**: TASK-0192

## 完了条件

- [ ] RecipeCardViewクラスが実装されている
- [ ] カード名が表示される
- [ ] APコストが表示される
- [ ] 必要素材が表示される
- [ ] 生成アイテムが表示される

---

## 実装詳細

### 1. RecipeCardView実装 🟡

**信頼性**: 🟡 *GatheringCardViewを参考*

**実装ファイル**: `src/presentation/phaser/ui/card/RecipeCardView.ts`

```typescript
import Phaser from 'phaser';
import { ICardView, CardViewOptions } from './ICardView';
import { CardState, CardStateStyles } from './CardState';
import { CardSize, CardSizeType } from './CardConstants';
import { getCardTypeDisplayOption } from './CardTypeOptions';
import { RecipeCard } from '../../../../domain/card/RecipeCard';
import { TextStyles } from '../../config/TextStyles';

export class RecipeCardView implements ICardView {
  public readonly container: Phaser.GameObjects.Container;
  public readonly card: RecipeCard;

  private scene: Phaser.Scene;
  private size: CardSizeType;
  private state: CardState;
  private background!: Phaser.GameObjects.Graphics;
  private nameText!: Phaser.GameObjects.Text;
  private costText!: Phaser.GameObjects.Text;
  private materialsText!: Phaser.GameObjects.Text;
  private outputText!: Phaser.GameObjects.Text;
  private onClick?: (card: RecipeCard) => void;
  private onHover?: (card: RecipeCard, isHovering: boolean) => void;

  constructor(scene: Phaser.Scene, options: CardViewOptions) {
    this.scene = scene;
    this.card = options.card as RecipeCard;
    this.size = options.size ?? 'STANDARD';
    this.state = options.state ?? 'normal';
    this.onClick = options.onClick as any;
    this.onHover = options.onHover as any;

    const { width, height } = CardSize[this.size];

    // コンテナ作成
    this.container = scene.add.container(options.x, options.y);

    // 背景
    this.background = scene.add.graphics();
    this.container.add(this.background);

    // カード種別の色を取得
    const typeOption = getCardTypeDisplayOption('recipe');

    // 背景描画
    this.drawBackground(typeOption.backgroundColor, typeOption.borderColor);

    // 種別ラベル
    const typeLabel = scene.add.text(
      -width/2 + 8, -height/2 + 8,
      '調合',
      { ...TextStyles.bodySmall, fontSize: '10px', color: typeOption.labelColor }
    );
    this.container.add(typeLabel);

    // APコスト
    this.costText = scene.add.text(
      width/2 - 15, -height/2 + 8,
      `${this.card.apCost}`,
      TextStyles.cardCost
    ).setOrigin(1, 0);
    this.container.add(this.costText);

    // カード名（生成アイテム名）
    this.nameText = scene.add.text(
      0, -height/2 + 35,
      this.card.outputItem.name,
      { ...TextStyles.cardName, wordWrap: { width: width - 16 } }
    ).setOrigin(0.5, 0);
    this.container.add(this.nameText);

    // 必要素材ラベル
    scene.add.text(
      -width/2 + 8, -height/2 + 60,
      '必要素材:',
      { ...TextStyles.bodySmall, fontSize: '9px', color: '#aaaaaa' }
    ).setOrigin(0, 0);

    // 必要素材リスト
    const materialsStr = this.card.requiredMaterials
      .map(m => `・${m.material.name}`)
      .slice(0, 3)
      .join('\n');
    this.materialsText = scene.add.text(
      -width/2 + 12, -height/2 + 75,
      materialsStr,
      { ...TextStyles.cardDescription, fontSize: '10px' }
    ).setOrigin(0, 0);
    this.container.add(this.materialsText);

    // 生成アイテム品質
    const qualityStr = this.card.outputItem.quality
      ? `品質: ${this.card.outputItem.quality}`
      : '';
    this.outputText = scene.add.text(
      0, height/2 - 25,
      qualityStr,
      { ...TextStyles.bodySmall, fontSize: '10px', color: '#ffd700' }
    ).setOrigin(0.5, 0);
    this.container.add(this.outputText);

    // インタラクション設定
    if (options.interactive !== false) {
      this.setInteractive(true);
    }

    // 初期状態適用
    this.applyState();
  }

  getState(): CardState {
    return this.state;
  }

  setState(state: CardState): void {
    this.state = state;
    this.applyState();
  }

  update(card: RecipeCard): void {
    this.nameText.setText(card.outputItem.name);
    this.costText.setText(`${card.apCost}`);
  }

  setPosition(x: number, y: number): void {
    this.container.setPosition(x, y);
  }

  setScale(scale: number): void {
    this.container.setScale(scale);
  }

  setAlpha(alpha: number): void {
    this.container.setAlpha(alpha);
  }

  setInteractive(enabled: boolean): void {
    if (enabled) {
      const { width, height } = CardSize[this.size];
      const hitArea = new Phaser.Geom.Rectangle(-width/2, -height/2, width, height);

      this.container.setInteractive(hitArea, Phaser.Geom.Rectangle.Contains);
      this.container.on('pointerover', () => {
        if (this.state !== 'disabled' && this.state !== 'selected') {
          this.setState('hover');
          if (this.onHover) this.onHover(this.card, true);
        }
      });
      this.container.on('pointerout', () => {
        if (this.state === 'hover') {
          this.setState('normal');
          if (this.onHover) this.onHover(this.card, false);
        }
      });
      this.container.on('pointerdown', () => {
        if (this.state !== 'disabled' && this.onClick) {
          this.onClick(this.card);
        }
      });
    } else {
      this.container.disableInteractive();
    }
  }

  setSelected(selected: boolean): void {
    this.setState(selected ? 'selected' : 'normal');
  }

  destroy(): void {
    this.container.destroy();
  }

  private drawBackground(bgColor: number, borderColor: number): void {
    const { width, height } = CardSize[this.size];
    const style = CardStateStyles[this.state];

    this.background.clear();
    this.background.fillStyle(bgColor, style.alpha);
    this.background.fillRoundedRect(-width/2, -height/2, width, height, 8);
    this.background.lineStyle(style.borderWidth, borderColor);
    this.background.strokeRoundedRect(-width/2, -height/2, width, height, 8);
  }

  private applyState(): void {
    const style = CardStateStyles[this.state];
    const typeOption = getCardTypeDisplayOption('recipe');

    this.drawBackground(typeOption.backgroundColor, typeOption.borderColor);
    this.container.setScale(style.scale);
    this.container.setAlpha(style.alpha);
  }
}
```

---

## 単体テスト要件

### テストケース1: カード生成 🟡

**Given**: RecipeCardデータがある
**When**: RecipeCardViewを生成する
**Then**: カードが表示される

### テストケース2: 必要素材表示 🟡

**Given**: RecipeCardViewがある
**When**: 必要素材を確認する
**Then**: 素材リストが表示されている

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-0191` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- 必要素材が多い場合の省略表示
- 品質表示の色分け
- カードサイズごとのレイアウト調整

---

## 信頼性レベルサマリー

- **総項目数**: 1項目
- 🔵 **青信号**: 0項目 (0%)
- 🟡 **黄信号**: 1項目 (100%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 中品質

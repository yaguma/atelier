# TASK-0151: E2Eテスト環境構築

**タスクID**: TASK-0151
**タスクタイプ**: DIRECT
**推定工数**: 4時間
**フェーズ**: Phase 7 - E2Eテスト
**信頼性レベル**: 🔵 *Phase 5タスク参照*

## 関連文書

- **概要**: [📋 overview.md](../atelier-guild-rank-overview.md)
- **要件定義**: [📋 requirements.md](../../spec/atelier-guild-rank/requirements.md)
- **Phase 5**: [📋 atelier-guild-rank-phase5.md](../atelier-guild-rank-phase5.md) - TASK-0136相当

## タスク概要

PlaywrightまたはCypressを使用したE2Eテスト環境を構築する。ブラウザでの実際のゲーム操作をテストできるようにする。

## 依存タスク

- **前提タスク**: TASK-0150
- **後続タスク**: TASK-0152, TASK-0153

## 完了条件

- [ ] Playwrightのインストールと設定
- [ ] playwright.config.tsの作成
- [ ] 基本的なE2Eテストが実行可能
- [ ] CI/CD統合用スクリプト

---

## 実装詳細

### 1. Playwrightインストール 🔵

**信頼性**: 🔵 *標準的なセットアップ*

```bash
npm install -D @playwright/test
npx playwright install
```

### 2. 設定ファイル作成 🔵

**信頼性**: 🔵 *標準構成*

```typescript
// playwright.config.ts
import { defineConfig } from '@playwright/test';

export default defineConfig({
  testDir: './tests/e2e',
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 2 : 0,
  workers: process.env.CI ? 1 : undefined,
  reporter: 'html',
  use: {
    baseURL: 'http://localhost:5173',
    trace: 'on-first-retry',
  },
  webServer: {
    command: 'npm run dev',
    url: 'http://localhost:5173',
    reuseExistingServer: !process.env.CI,
  },
});
```

### 3. ディレクトリ構造 🔵

```
tests/
├── e2e/
│   ├── title.spec.ts
│   ├── game-flow.spec.ts
│   ├── shop.spec.ts
│   └── fixtures/
└── ...
```

### 4. npm scripts追加 🔵

```json
{
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui"
  }
}
```

---

## 実装手順

1. `/tsumiki:direct-setup` - 直接実装・設定
2. `/tsumiki:direct-verify` - 動作確認・品質確認

---

## 注意事項

- 開発サーバーが起動していることが前提
- ブラウザ自動インストールが必要

---

## 信頼性レベルサマリー

- **総項目数**: 4項目
- 🔵 **青信号**: 4項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質

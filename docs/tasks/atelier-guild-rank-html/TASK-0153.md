# TASK-0153: 1ターンサイクルE2Eテスト

**タスクID**: TASK-0153
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 7 - E2Eテスト
**信頼性レベル**: 🟡 *TASK-0138相当*

## 関連文書

- **概要**: [📋 overview.md](../atelier-guild-rank-overview.md)
- **Phase 5**: [📋 atelier-guild-rank-phase5.md](../atelier-guild-rank-phase5.md) - TASK-0138相当

## タスク概要

依頼受注→ドラフト採取→調合→納品の1ターンサイクルをE2Eテストで確認する。

## 依存タスク

- **前提タスク**: TASK-0152
- **後続タスク**: TASK-0154

## 完了条件

- [x] 依頼受注フェーズのテスト（スケルトン作成、UIとの調整待ち）
- [x] ドラフト採取フェーズのテスト（スケルトン作成、UIとの調整待ち）
- [x] 調合フェーズのテスト（スケルトン作成、UIとの調整待ち）
- [x] 納品フェーズのテスト（スケルトン作成、UIとの調整待ち）
- [x] 日経過のテスト（スケルトン作成、UIとの調整待ち）

**注意**: テストスケルトンは作成済み。UIの実装（依頼受注必須でフェーズ遷移）に合わせた調整が必要。

---

## 実装詳細

### 1. 1ターンサイクルテスト 🟡

**信頼性**: 🟡 *フロー確認*

```typescript
// tests/e2e/game-flow.spec.ts
import { test, expect } from '@playwright/test';

test.describe('ゲームフロー', () => {
  test.beforeEach(async ({ page }) => {
    await page.goto('/');
    await page.click('[data-testid="new-game-button"]');
  });

  test('1ターンサイクルを完了できる', async ({ page }) => {
    // 依頼受注
    await expect(page.locator('[data-testid="quest-phase"]')).toBeVisible();
    await page.click('[data-testid="quest-item-0"]');
    await page.click('[data-testid="accept-quest-button"]');

    // ドラフト採取
    await expect(page.locator('[data-testid="gathering-phase"]')).toBeVisible();
    await page.click('[data-testid="material-option-0"]');
    // ... ドラフト完了まで

    // 調合
    await expect(page.locator('[data-testid="alchemy-phase"]')).toBeVisible();
    await page.click('[data-testid="recipe-item-0"]');
    await page.click('[data-testid="craft-button"]');

    // 納品
    await expect(page.locator('[data-testid="delivery-phase"]')).toBeVisible();
    await page.click('[data-testid="deliver-button"]');

    // 日経過確認
    await expect(page.locator('[data-testid="day-counter"]')).toContainText('2');
  });
});
```

**実装ファイル**: `tests/e2e/game-flow.spec.ts`

---

## 単体テスト要件

### テストケース1: フェーズ遷移 🟡

各フェーズへの正常な遷移を確認

### テストケース2: 状態更新 🟡

日数、ゴールド、インベントリの更新を確認

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-0153` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- テストの実行時間が長くなる可能性
- 非同期処理の待機を適切に設定

---

## 信頼性レベルサマリー

- **総項目数**: 4項目
- 🔵 **青信号**: 0項目 (0%)
- 🟡 **黄信号**: 4項目 (100%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 要改善（実際のUI確認が必要）

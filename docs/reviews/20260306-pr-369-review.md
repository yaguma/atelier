# Team Code Review Report (Lite)

> Reviewed by: Code Quality, Safety, QA & Performance

## Overview

| Perspective | Critical | Warning | Info |
|-------------|----------|---------|------|
| Code Quality | 0 | 1 | 1 |
| Safety | 0 | 0 | 1 |
| QA & Performance | 0 | 1 | 1 |
| **Total** | **0** | **2** | **3** |

---

No critical issues found.

---

## Warning Issues

<details>
<summary>Warning Issues (2 items) - Click to expand</summary>

### 1. handleGameOver と handleGameCleared のロジック重複

- **Perspective**: Code Quality
- **Severity**: Warning (コード品質)
- **File**: `atelier-guild-rank/src/scenes/MainScene.ts:293-317`
- **Issue**: `handleGameOver()` と `handleGameCleared()` はシーン遷移先の文字列（`'GameOverScene'` vs `'GameClearScene'`）のみが異なり、GameEndStats構築ロジックが完全に重複している。DRY原則に反する。
- **Suggested Fix**: 共通のヘルパーメソッドを抽出する。
  ```typescript
  private buildGameEndStats(condition: GameEndCondition): GameEndStats {
    const state = this.stateManager.getState();
    return {
      finalRank: condition.finalRank,
      totalDays: condition.totalDays,
      totalDeliveries: 0, // TODO: 実際の納品数をStateから取得
      totalGold: state.gold,
    };
  }

  private handleGameOver(condition: GameEndCondition): void {
    this.scene.start('GameOverScene', { stats: this.buildGameEndStats(condition) });
  }

  private handleGameCleared(condition: GameEndCondition): void {
    this.scene.start('GameClearScene', { stats: this.buildGameEndStats(condition) });
  }
  ```

### 2. テストファイルのモック定義が大量（522行中 約300行がモック）

- **Perspective**: QA & Performance
- **Severity**: Warning (テスト保守性)
- **File**: `atelier-guild-rank/tests/unit/presentation/scenes/main-scene-game-end.test.ts:20-308`
- **Issue**: テストファイル522行のうち約300行がモック定義で占められている。既存のMainSceneテストファイルがある場合、モック定義を共通化すべき。現状では同様のモック定義が複数テストファイルに重複する可能性がある。
- **Suggested Fix**: `tests/mocks/` 配下に共通のPhaserシーンモックファクトリを作成し、複数のMainSceneテストファイルで再利用可能にする。例: `tests/mocks/phaser-scene-mock.ts`

</details>

---

## Info & Suggestions

<details>
<summary>Info (3 items) - Click to expand</summary>

- [Code Quality] `totalDeliveries: 0` のTODOコメントが本体コード（MainScene.ts:298, 313）とdebug.ts（443, 468）の計4箇所に存在する。Issue化して一括対応を推奨。(`atelier-guild-rank/src/scenes/MainScene.ts:298,313`)
- [Safety] GAME_OVER/GAME_CLEAREDイベントの二重発火を防ぐガードがない。イベントバスの実装次第では、短時間に複数回遷移が呼ばれる可能性がある。現時点ではPhaserのscene.start()が冪等に振る舞うため実害は低いが、フラグによるガードを検討してもよい。(`atelier-guild-rank/src/scenes/MainScene.ts:293-317`)
- [QA & Performance] GAME_CLEAREDイベントのshutdown後の購読解除テストがない。GAME_OVERのshutdownテストは存在するが、GAME_CLEAREDについても同様のテストを追加するとカバレッジが向上する。(`atelier-guild-rank/tests/unit/presentation/scenes/main-scene-game-end.test.ts:492-521`)

</details>

---

## Good Points

- 既存のイベント購読パターン（`unsubscribeHandlers`配列へのpush + shutdown時の一括解除）に忠実に従っており、メモリリークのリスクが低い実装になっている。

---

*Generated by PR Review Team (Lite) - 3 specialized reviewers + Team Lead*
*🤖 [Claude Code](https://claude.com/claude-code)*

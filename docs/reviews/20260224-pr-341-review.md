# Team Code Review Report (Lite)

> Reviewed by: Code Quality, Safety, QA & Performance

## PR Information

- **PR**: #341
- **Title**: docs: core-systems-infrastructure.mdのEventBus設計・バージョン記述を最新化
- **Branch**: docs/#337-update-core-systems-infrastructure -> main
- **Changes**: +143 / -91 (2 files)
- **Review Date**: 2026-02-24
- **Mode**: Lite + Quick

## Overview

| Perspective | Critical | Warning | Info |
|-------------|----------|---------|------|
| Code Quality | 0 | 1 | 2 |
| Safety | 0 | 0 | 0 |
| QA & Performance | 0 | 0 | 1 |
| **Total** | **0** | **1** | **3** |

---

No critical issues found.

---

## Warning Issues

### 1. StateManagerクラス図がコードサンプルと不整合

- **Perspective**: Code Quality
- **Severity**: Warning (W1 - アーキテクチャ違反)
- **File**: `docs/design/atelier-guild-rank/core-systems-infrastructure.md:520-541`
- **Issue**: セクション6.2のStateManagerクラス図では旧インターフェース（`subscribe(key, callback)`、`unsubscribe(key, callback)`、`-notifyChange(key, value)`、`-subscribers: Map<string, Set<Function>>`）が定義されているが、同ファイル内のセクション6.3のコードサンプル（行546-574）はEventBus注入パターンに正しく更新されている。クラス図とコードサンプルの間に矛盾が存在する。実装コード（`StateManager.ts`）では`subscribe`/`unsubscribe`メソッドは存在せず、`IEventBus`をコンストラクタ注入して`notifyChanges`（複数形）でイベントを発行するパターンになっている。
- **Suggested Fix**: セクション6.2のクラス図を以下のように更新する:
  - `subscribe`/`unsubscribe`メソッドを削除
  - `-notifyChange` を `-notifyChanges(previousState: IGameState): void` に変更
  - `-subscribers: Map<string, Set<Function>>` を `-readonly eventBus: IEventBus` に変更
  - コンストラクタに `+constructor(eventBus: IEventBus)` を追加

---

## Info Issues

### 1. DI登録コードサンプルでconst eventBusの重複宣言

- **Perspective**: Code Quality
- **Severity**: Info (I2)
- **File**: `docs/design/atelier-guild-rank/core-systems-infrastructure.md:63-75`
- **Issue**: セクション2.4のDI登録コードサンプルで `const eventBus` が2回宣言されている（65行目の `const eventBus = new EventBus()` と74行目の `const eventBus = container.resolve<IEventBus>(...)`)。1つのスコープ内での重複宣言はTypeScriptコンパイルエラーになるため、利用側の例は別のブロックまたはコメントで分離すると読みやすい。

### 2. DAY_STARTEDイベントの発火元の明確化

- **Perspective**: Code Quality
- **Severity**: Info (I2)
- **File**: `docs/design/atelier-guild-rank/core-systems-infrastructure.md:85`
- **Issue**: セクション2.5のイベント定義表で `DAY_STARTED` の発火元が `GameFlowManager` となっているが、実装コード（`StateManager.ts:129`）では `StateManager.advanceDay()` 内で `DAY_STARTED` イベントを発行している。設計上の発火元をどちらにするかの明確化が望ましい。

### 3. ペイロード型のインターフェース名参照

- **Perspective**: QA & Performance
- **Severity**: Info (I2)
- **File**: `docs/design/atelier-guild-rank/core-systems-infrastructure.md:81-114`
- **Issue**: セクション2.5のイベント定義表はイベント種別を網羅的にリストしているが、各ペイロード型が簡略表記（`{ day }` 等）になっている。実装コード（`events.ts`）では `IPhaseChangedEvent` 等の厳密なインターフェースが定義されているため、設計書からも型インターフェース名を参照できるとトレーサビリティが向上する。

---

## Good Points

- EventBusセクション（2.1-2.6）の全面更新は実装コードと非常に良く整合しており、設計方針・クラス図・DI登録・イベント定義・使用例のすべてが実装を正確に反映している

---

*Generated by PR Review Team (Lite) - 3 specialized reviewers + Team Lead*
*Generated with [Claude Code](https://claude.com/claude-code)*

# TASK-0046 要件定義書: MainScene共通レイアウト実装

**バージョン**: 1.0.0
**作成日**: 2026-01-21
**タスクID**: TASK-0046
**カテゴリ**: UI層（Presentation）

---

## 1. 概要

### 1.1 目的

MainSceneの共通レイアウト（ヘッダー、サイドバー、フッター、コンテンツエリア）を実装し、4つのゲームフェーズに共通するUI基盤を提供する。

### 1.2 背景

TASK-0020（MainScene共通レイアウト）は完了とマークされていたが、実際のMainScene.tsはスタブ実装のままである。本タスクでフルレイアウトに置き換える必要がある。

### 1.3 スコープ

| 含まれる | 含まれない |
|---------|-----------|
| ヘッダーUI（HeaderUI）コンポーネント | 各フェーズUIの詳細実装 |
| サイドバーUI（SidebarUI）コンポーネント | ショップボタンのクリック処理 |
| フッターUI（FooterUI）コンポーネント | 手札表示の詳細実装 |
| MainSceneのレイアウト構成 | ダイアログ・トースト実装 |
| StateManagerとの連携 | アニメーション実装 |
| フェーズUI切り替え機能 | レスポンシブ対応 |

---

## 2. 機能要件（EARS記法）

### 2.1 MainScene初期化

#### REQ-0046-001: MainScene生成時のレイアウト作成

**When** MainSceneのcreate()メソッドが呼び出される
**The system shall** 以下のコンポーネントを生成する:
1. ヘッダーUI（画面上部）
2. サイドバーUI（画面左側）
3. フッターUI（画面下部）
4. コンテンツエリア（中央部）

**信頼性**: 🔵 青信号（設計文書に記載）

#### REQ-0046-002: サービス初期化

**When** MainSceneのcreate()メソッドが呼び出される
**The system shall** 以下のサービスへの参照を取得する:
1. StateManager（状態管理）
2. GameFlowManager（フロー管理）
3. EventBus（イベント通信）

**信頼性**: 🔵 青信号（設計文書に記載）

---

### 2.2 ヘッダーUI（HeaderUI）

#### REQ-0046-010: ランク表示

**The system shall** 現在のギルドランク（G〜S）をヘッダー左側に表示する

**表示形式**: `ランク: {rank}`（例: `ランク: E`）

**信頼性**: 🔵 青信号

#### REQ-0046-011: 昇格ゲージ表示

**The system shall** 昇格ゲージ（0〜100）をプログレスバー形式で表示する

**表示形式**: プログレスバー + `{current}/{max}`（例: `35/100`）

**ゲージ色の閾値**:
| 閾値 | 色 | 説明 |
|------|-----|------|
| 0〜30% | 赤系（0xFF6B6B） | 低い |
| 30〜60% | 黄系（0xFFD93D） | 中程度 |
| 60〜100% | 緑系（0x6BCB77） | 高い |
| 100% | 水色（0x4ECDC4） | 満タン |

**信頼性**: 🔵 青信号

#### REQ-0046-012: 残り日数表示

**The system shall** 残り日数をヘッダーに表示する

**表示形式**: `残り: {remainingDays}日`

**緊急度による色変化**:
| 残り日数 | 色 | 効果 |
|---------|-----|------|
| 11日以上 | 白（0xFFFFFF） | 通常表示 |
| 6〜10日 | 黄（0xFFD93D） | 警告表示 |
| 4〜5日 | 赤（0xFF6B6B） | 緊急表示 |
| 1〜3日 | 赤（0xFF0000） | 点滅表示 |

**信頼性**: 🔵 青信号

#### REQ-0046-013: 所持金表示

**The system shall** 所持ゴールドをヘッダーに表示する

**表示形式**: `{gold}G`（例: `500G`）

**信頼性**: 🔵 青信号

#### REQ-0046-014: 行動ポイント表示

**The system shall** 行動ポイント（AP）をヘッダー右側に表示する

**表示形式**: `{current}/{max} AP`（例: `3/3 AP`）

**信頼性**: 🔵 青信号

#### REQ-0046-015: ヘッダー状態更新

**When** StateManagerの状態が変更される
**The system shall** ヘッダーの表示を更新する

**更新対象**: ランク、昇格ゲージ、残り日数、所持金、行動ポイント

**信頼性**: 🔵 青信号

---

### 2.3 サイドバーUI（SidebarUI）

#### REQ-0046-020: 受注依頼リスト表示

**The system shall** 受注済み依頼をアコーディオンセクションで表示する

**表示情報**（各依頼）:
- アイテム名と必要数量
- 現在の進捗（納品済み数/必要数）
- 残り期限（日数）
- 報酬（ゴールド、貢献度）

**信頼性**: 🔵 青信号

#### REQ-0046-021: 素材リスト表示

**The system shall** 所持素材をアコーディオンセクションで表示する

**表示情報**（各素材）:
- 素材名
- 数量
- 品質（D〜S）

**信頼性**: 🔵 青信号

#### REQ-0046-022: 完成品リスト表示

**The system shall** 調合済みアイテムをアコーディオンセクションで表示する

**表示情報**（各アイテム）:
- アイテム名
- 数量
- 品質（D〜S）

**信頼性**: 🔵 青信号

#### REQ-0046-023: 保管容量表示

**The system shall** 現在の保管数と最大容量を表示する

**表示形式**: `保管: {current}/{max}`（例: `保管: 12/20`）

**信頼性**: 🟡 黄信号（設計文書から妥当な推測）

#### REQ-0046-024: ショップボタン表示

**The system shall** ショップへのアクセスボタンをサイドバー下部に表示する

**信頼性**: 🔵 青信号

---

### 2.4 フッターUI（FooterUI）

#### REQ-0046-030: フェーズインジケーター表示

**The system shall** 4フェーズの進行状況をステッパー形式で表示する

**フェーズ順序**:
1. 依頼受注（QUEST_ACCEPT）
2. 採取（GATHERING）
3. 調合（ALCHEMY）
4. 納品（DELIVERY）

**表示状態**:
| 状態 | 表示 | 説明 |
|------|------|------|
| PENDING | グレーアウト | 未到達 |
| CURRENT | ハイライト | 現在のフェーズ |
| COMPLETED | チェックマーク | 完了 |
| SKIPPED | 斜線 | スキップ |

**信頼性**: 🔵 青信号

#### REQ-0046-031: 現在フェーズハイライト

**The system shall** 現在のフェーズをハイライト表示する

**When** フェーズが変更される
**The system shall** インジケーターの表示状態を更新する

**信頼性**: 🔵 青信号

#### REQ-0046-032: 手札表示エリア

**The system shall** 手札表示エリアをフッターに配置する

**表示**: 最大5枚のカードを横並びで表示可能なエリア

**注意**: 手札の詳細実装は本タスクのスコープ外

**信頼性**: 🔵 青信号

#### REQ-0046-033: 次へボタン表示

**The system shall** 次フェーズへ進むボタンをフッター右側に表示する

**ラベル変更**:
| 現在フェーズ | ボタンラベル |
|-------------|------------|
| QUEST_ACCEPT | 採取へ |
| GATHERING | 調合へ |
| ALCHEMY | 納品へ |
| DELIVERY | 日終了 |

**信頼性**: 🔵 青信号

---

### 2.5 コンテンツエリア

#### REQ-0046-040: フェーズUI配置

**The system shall** 各フェーズのUIコンポーネントを格納するコンテナを提供する

**配置**: ヘッダー下、サイドバー右、フッター上のエリア

**信頼性**: 🔵 青信号

#### REQ-0046-041: フェーズUI切り替え

**When** ゲームフェーズが変更される
**The system shall** 対応するフェーズUIのみを表示し、他を非表示にする

**信頼性**: 🔵 青信号

---

### 2.6 イベント連携

#### REQ-0046-050: フェーズ変更イベント購読

**The system shall** PHASE_CHANGEDイベントを購読し、UI更新を行う

**信頼性**: 🔵 青信号

#### REQ-0046-051: 日開始イベント購読

**The system shall** DAY_STARTEDイベントを購読し、日数表示を更新する

**信頼性**: 🔵 青信号

#### REQ-0046-052: 状態変更イベント購読

**The system shall** StateManagerの状態変更を監視し、ヘッダー・サイドバーを更新する

**信頼性**: 🔵 青信号

---

## 3. 画面レイアウト仕様

### 3.1 基本レイアウト

```
┌─────────────────────────────────────────────────────────────────────────┐
│ ┌───────────────────────────────────────────────────────────────────┐   │
│ │ ヘッダー (HeaderUI) - 高さ: 60px                                  │   │
│ │ [ランク: E] [昇格ゲージ ████ 35/100] [残り: 25日] [500G] [3/3 AP]  │   │
│ └───────────────────────────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────────────────────┤
│ ┌─────────────┐ ┌───────────────────────────────────────────────────┐   │
│ │ サイドバー  │ │                                                   │   │
│ │ (SidebarUI) │ │                                                   │   │
│ │ 幅: 200px   │ │        コンテンツエリア (contentContainer)        │   │
│ │             │ │                                                   │   │
│ │ 【受注依頼】│ │        各フェーズUIがここに表示される              │   │
│ │ 【素材】    │ │                                                   │   │
│ │ 【完成品】  │ │                                                   │   │
│ │ 保管: 12/20│ │                                                   │   │
│ │ [ショップ] │ │                                                   │   │
│ └─────────────┘ └───────────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────────────────────┤
│ ┌───────────────────────────────────────────────────────────────────┐   │
│ │ フッター (FooterUI) - 高さ: 120px                                 │   │
│ │ [依頼受注]●━━○[採取]━━○[調合]━━○[納品]                          │   │
│ │ 手札エリア: [    ][    ][    ][    ][    ]           [次へ]       │   │
│ └───────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
```

### 3.2 座標・サイズ仕様

| 要素 | 位置 | サイズ |
|------|------|--------|
| **画面全体** | - | 1280 x 720 px |
| **ヘッダー** | 上部 (y=0) | 1280 x 60 px |
| **サイドバー** | 左側 (x=0, y=60) | 200 x 540 px |
| **コンテンツエリア** | 中央 (x=200, y=60) | 1080 x 540 px |
| **フッター** | 下部 (y=600) | 1280 x 120 px |

### 3.3 深度（Z-Index）設定

| レイヤー | depth値 | 内容 |
|---------|---------|------|
| Background | 0 | 背景 |
| Content | 100 | コンテンツエリア |
| Sidebar | 150 | サイドバー |
| Header/Footer | 200 | ヘッダー・フッター |
| Overlay | 300 | オーバーレイ |
| Dialog | 400 | ダイアログ |

---

## 4. インターフェース定義

### 4.1 HeaderUI

```typescript
/**
 * ヘッダーUI更新用データ
 */
interface HeaderUIData {
  currentRank: GuildRank;
  promotionGauge: number;
  remainingDays: number;
  gold: number;
  actionPoints: number;
  maxActionPoints: number;
}

/**
 * ヘッダーUIコンポーネント
 */
interface IHeaderUI {
  /** コンポーネント初期化 */
  create(): void;

  /** コンポーネント破棄 */
  destroy(): void;

  /** 表示データを更新 */
  update(data: HeaderUIData): void;
}
```

### 4.2 SidebarUI

```typescript
/**
 * サイドバーUI更新用データ
 */
interface SidebarUIData {
  activeQuests: IActiveQuest[];
  materials: IMaterialInstance[];
  craftedItems: ICraftedItem[];
  currentStorage: number;
  maxStorage: number;
}

/**
 * サイドバーUIコンポーネント
 */
interface ISidebarUI {
  /** コンポーネント初期化 */
  create(): void;

  /** コンポーネント破棄 */
  destroy(): void;

  /** 表示データを更新 */
  update(data: SidebarUIData): void;

  /** セクションの折りたたみ切替 */
  toggleSection(sectionId: string): void;
}
```

### 4.3 FooterUI

```typescript
/**
 * フッターUI更新用データ
 */
interface FooterUIData {
  currentPhase: GamePhase;
  completedPhases: GamePhase[];
  nextButtonLabel: string;
  isNextButtonEnabled: boolean;
}

/**
 * フッターUIコンポーネント
 */
interface IFooterUI {
  /** コンポーネント初期化 */
  create(): void;

  /** コンポーネント破棄 */
  destroy(): void;

  /** フェーズインジケーター更新 */
  updatePhaseIndicator(currentPhase: GamePhase, completedPhases: GamePhase[]): void;

  /** 次へボタン更新 */
  updateNextButton(label: string, enabled: boolean): void;

  /** 次へボタンクリックハンドラ設定 */
  onNextClick(callback: () => void): void;
}
```

### 4.4 MainScene拡張

```typescript
/**
 * MainSceneに追加するメソッド
 */
interface IMainSceneExtension {
  /** レイアウト作成 */
  createLayout(): void;

  /** フェーズUI作成 */
  createPhaseUIs(): void;

  /** イベント購読設定 */
  subscribeToEvents(): void;

  /** 現在フェーズのUI表示 */
  showCurrentPhase(): void;

  /** フェーズ切り替え */
  showPhase(phase: GamePhase): void;

  /** ヘッダー更新 */
  updateHeader(): void;

  /** フッター更新 */
  updateFooter(): void;
}
```

---

## 5. 受け入れ基準

### 5.1 必須条件（Must Have）

| ID | 基準 | 検証方法 |
|----|------|----------|
| AC-001 | MainSceneが起動時にレイアウト（ヘッダー・サイドバー・フッター）を表示する | ユニットテスト |
| AC-002 | ヘッダーにランク・昇格ゲージ・日数・所持金・APが表示される | ユニットテスト |
| AC-003 | サイドバーに依頼・素材・完成品の3セクションが表示される | ユニットテスト |
| AC-004 | フッターにフェーズインジケーター（4フェーズ）が表示される | ユニットテスト |
| AC-005 | フェーズ変更時にコンテンツエリアのUIが切り替わる | ユニットテスト |
| AC-006 | StateManager状態変更時にヘッダーが更新される | ユニットテスト |
| AC-007 | 各コンポーネントがBaseComponentを継承している | 静的解析 |

### 5.2 推奨条件（Should Have）

| ID | 基準 | 検証方法 |
|----|------|----------|
| AC-010 | 残り日数5日以下で警告色表示 | ユニットテスト |
| AC-011 | 昇格ゲージが閾値に応じて色変化 | ユニットテスト |
| AC-012 | サイドバーセクションが折りたたみ可能 | 手動テスト |

### 5.3 除外条件（Won't Have）

| ID | 内容 | 理由 |
|----|------|------|
| EX-001 | サイドバー折りたたみ機能 | 後続タスクで対応 |
| EX-002 | フェーズ遷移アニメーション | 後続タスクで対応 |
| EX-003 | レスポンシブ対応 | 後続タスクで対応 |

---

## 6. テストケースの前提条件

### 6.1 テスト環境

- テストフレームワーク: Vitest
- モック: Phaserシーン、rexUIプラグイン、StateManager
- テストファイル: `tests/unit/presentation/main-scene.test.ts`

### 6.2 モック要件

#### Phaserシーンモック

```typescript
// 必要なモックオブジェクト
const mockScene = {
  add: {
    container: vi.fn(),
    text: vi.fn(),
    rectangle: vi.fn(),
  },
  cameras: {
    main: {
      centerX: 640,
      centerY: 360,
      width: 1280,
      height: 720,
    },
  },
  rexUI: {
    add: {
      sizer: vi.fn(),
      label: vi.fn(),
      roundRectangle: vi.fn(),
      scrollablePanel: vi.fn(),
    },
  },
};
```

#### StateManagerモック

```typescript
// 初期状態モック
const mockGameState: IGameState = {
  currentRank: 'E',
  promotionGauge: 35,
  remainingDays: 25,
  currentDay: 6,
  currentPhase: 'QUEST_ACCEPT',
  gold: 500,
  actionPoints: 3,
  comboCount: 0,
  rankHp: 100,
  isPromotionTest: false,
};
```

### 6.3 テストデータ

#### 受注依頼テストデータ

```typescript
const mockActiveQuests: IActiveQuest[] = [
  {
    questId: 'quest-001',
    itemId: 'potion-001',
    requiredQuantity: 2,
    currentQuantity: 1,
    deadline: 3,
    reward: { gold: 50, contribution: 10 },
  },
];
```

#### 素材テストデータ

```typescript
const mockMaterials: IMaterialInstance[] = [
  { materialId: 'herb', name: '薬草', quantity: 5, quality: 'C' },
  { materialId: 'water', name: '清水', quantity: 3, quality: 'B' },
];
```

---

## 7. 依存関係

### 7.1 前提タスク

| タスクID | タスク名 | 状態 |
|---------|---------|------|
| TASK-0018 | BaseComponent実装 | 完了 |
| TASK-0017 | GameFlowManager実装 | 完了 |
| TASK-0005 | StateManager実装 | 完了 |

### 7.2 後続タスク（影響を与える）

| タスクID | タスク名 | 影響 |
|---------|---------|------|
| TASK-0021 | QuestAcceptPhaseUI | コンテンツエリアに配置 |
| TASK-0022 | GatheringPhaseUI | コンテンツエリアに配置 |
| TASK-0023 | AlchemyPhaseUI | コンテンツエリアに配置 |
| TASK-0024 | DeliveryPhaseUI | コンテンツエリアに配置 |

---

## 8. 成果物一覧

| 成果物 | パス | 説明 |
|--------|------|------|
| MainScene.ts | `/src/presentation/scenes/MainScene.ts` | メインシーン（再実装） |
| HeaderUI.ts | `/src/presentation/ui/components/HeaderUI.ts` | ヘッダーコンポーネント |
| SidebarUI.ts | `/src/presentation/ui/components/SidebarUI.ts` | サイドバーコンポーネント |
| FooterUI.ts | `/src/presentation/ui/components/FooterUI.ts` | フッターコンポーネント |
| main-scene.test.ts | `/tests/unit/presentation/main-scene.test.ts` | テストファイル |

---

## 9. 参考資料

### 9.1 設計文書

- **メイン画面設計**: `docs/design/atelier-guild-rank/ui-design/screens/main.md`
- **共通コンポーネント設計**: `docs/design/atelier-guild-rank/ui-design/screens/common-components.md`
- **UI設計概要**: `docs/design/atelier-guild-rank/ui-design/overview.md`

### 9.2 既存実装

- **BaseComponent**: `/src/presentation/ui/components/BaseComponent.ts`
- **TitleScene**: `/src/presentation/scenes/TitleScene.ts`（参考実装）
- **theme.ts**: `/src/presentation/ui/theme.ts`（テーマ定義）

### 9.3 タスク関連

- **タスクファイル**: `docs/tasks/atelier-guild-rank/phase-3/TASK-0046.md`
- **タスクノート**: `docs/implements/atelier-guild-rank/TASK-0046/note.md`

---

## 変更履歴

| 日付 | バージョン | 変更内容 |
|------|----------|---------|
| 2026-01-21 | 1.0.0 | 初版作成 |
